// Code generated by scalr-gen. DO NOT EDIT.

package webhook_integration_delivery

import (
	"context"
	"encoding/json"
	"fmt"
	"iter"
	"net/url"
	"strings"

	"github.com/scalr/go-scalr/v2/scalr/client"
	"github.com/scalr/go-scalr/v2/scalr/schemas"
)

// Client provides access to WebhookIntegrationDelivery operations
type Client struct {
	httpClient *client.HTTPClient
}

// New creates a new WebhookIntegrationDelivery client
func New(httpClient *client.HTTPClient) *Client {
	return &Client{httpClient: httpClient}
}

// This endpoint returns a single webhook integration delivery.
func (c *Client) GetWebhookIntegrationDeliveryRaw(ctx context.Context, webhookDelivery string, opts *GetWebhookIntegrationDeliveryOptions) (*client.Response, error) {
	path := "/integrations/webhook-deliveries/{webhook_delivery}"
	path = strings.ReplaceAll(path, "{webhook_delivery}", url.PathEscape(webhookDelivery))

	params := url.Values{}
	if opts != nil {
		if len(opts.Include) > 0 {
			params.Set("include", strings.Join(opts.Include, ","))
		}
		// Handle parameter: Fields (map[string]interface{})
		// Complex type map[string]interface{} - skip for now
		// Add filters
		for k, v := range opts.Filter {
			params.Set("filter["+k+"]", v)
		}
	}
	if len(params) > 0 {
		path += "?" + params.Encode()
	}

	httpResp, err := c.httpClient.Get(ctx, path, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// This endpoint returns a single webhook integration delivery.
func (c *Client) GetWebhookIntegrationDelivery(ctx context.Context, webhookDelivery string, opts *GetWebhookIntegrationDeliveryOptions) (*schemas.WebhookIntegrationDelivery, error) {
	resp, err := c.GetWebhookIntegrationDeliveryRaw(ctx, webhookDelivery, opts)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()

	var result struct {
		Data     schemas.WebhookIntegrationDelivery `json:"data"`
		Included []map[string]interface{}           `json:"included"`
	}
	if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
		return nil, fmt.Errorf("failed to decode response: %w", err)
	}

	// Populate included resources into relationships
	if len(result.Included) > 0 {
		result.Data.Relationships.PopulateIncludes(result.Included)
	}
	return &result.Data, nil
}

// GetWebhookIntegrationDeliveryOptions holds optional parameters for GetWebhookIntegrationDelivery
type GetWebhookIntegrationDeliveryOptions struct {
	// The comma-separated list of relationship paths.
	Include []string
	// The value of the fields[resource-type] parameter is a comma-separated list that refers to the name of the fields to be returned for the resource. An empty value indicates that no fields should be returned.
	Fields map[string]interface{}
	Filter map[string]string
}

// This endpoint returns a list of webhook integration deliveries filtered according to various criteria.
func (c *Client) ListWebhookIntegrationDeliveriesRaw(ctx context.Context, opts *ListWebhookIntegrationDeliveriesOptions) (*client.Response, error) {
	path := "/integrations/webhook-deliveries"

	params := url.Values{}
	if opts != nil {
		if opts.PageNumber > 0 {
			params.Set("page[number]", fmt.Sprintf("%d", opts.PageNumber))
		}
		if opts.PageSize > 0 {
			params.Set("page[size]", fmt.Sprintf("%d", opts.PageSize))
		}
		if len(opts.Include) > 0 {
			params.Set("include", strings.Join(opts.Include, ","))
		}
		if len(opts.Sort) > 0 {
			params.Set("sort", strings.Join(opts.Sort, ","))
		}
		// Handle parameter: Query (string)
		if opts.Query != "" {
			params.Set("query", opts.Query)
		}
		// Handle parameter: Fields (map[string]interface{})
		// Complex type map[string]interface{} - skip for now
		// Add filters
		for k, v := range opts.Filter {
			params.Set("filter["+k+"]", v)
		}
	}
	if len(params) > 0 {
		path += "?" + params.Encode()
	}

	httpResp, err := c.httpClient.Get(ctx, path, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// This endpoint returns a list of webhook integration deliveries filtered according to various criteria.
func (c *Client) ListWebhookIntegrationDeliveries(ctx context.Context, opts *ListWebhookIntegrationDeliveriesOptions) ([]*schemas.WebhookIntegrationDelivery, error) {
	resp, err := c.ListWebhookIntegrationDeliveriesRaw(ctx, opts)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()

	var result struct {
		Data []schemas.WebhookIntegrationDelivery `json:"data"`
		Meta struct {
			Pagination *client.Pagination `json:"pagination"`
		} `json:"meta"`
		Included []map[string]interface{} `json:"included"`
	}
	if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
		return nil, fmt.Errorf("failed to decode response: %w", err)
	}

	resources := make([]*schemas.WebhookIntegrationDelivery, len(result.Data))
	for i := range result.Data {
		resources[i] = &result.Data[i]
		// Populate included resources into relationships
		if len(result.Included) > 0 {
			resources[i].Relationships.PopulateIncludes(result.Included)
		}
	}
	return resources, nil
}

// ListWebhookIntegrationDeliveriesIter returns an iterator for paginated results using Go 1.23+ range over iter.Seq2 feature.
// This is the recommended and simple way to iterate through all the results.
//
// The iterator automatically fetches pages as needed and handles errors inline.
//
// Example:
//
//	for item, err := range client.WebhookIntegrationDelivery.ListWebhookIntegrationDeliveriesIter(ctx, opts) {
//	    if err != nil {
//	        return err
//	    }
//	    // Process item
//	}
func (c *Client) ListWebhookIntegrationDeliveriesIter(ctx context.Context, opts *ListWebhookIntegrationDeliveriesOptions) iter.Seq2[schemas.WebhookIntegrationDelivery, error] {
	return func(yield func(schemas.WebhookIntegrationDelivery, error) bool) {
		// Determine page size from opts or use default
		pageSize := 20
		if opts != nil && opts.PageSize > 0 {
			pageSize = opts.PageSize
		}

		pageNum := 1

		for {
			// Check context
			if err := ctx.Err(); err != nil {
				yield(schemas.WebhookIntegrationDelivery{}, err)
				return
			}

			// Copy options and set page number
			pageOpts := &ListWebhookIntegrationDeliveriesOptions{}
			if opts != nil {
				*pageOpts = *opts
			}
			pageOpts.PageNumber = pageNum
			pageOpts.PageSize = pageSize

			// Fetch page using Raw method to get pagination metadata
			resp, err := c.ListWebhookIntegrationDeliveriesRaw(ctx, pageOpts)
			if err != nil {
				yield(schemas.WebhookIntegrationDelivery{}, err)
				return
			}
			defer resp.Body.Close()

			// Decode response
			var result struct {
				Data []schemas.WebhookIntegrationDelivery `json:"data"`
				Meta struct {
					Pagination *client.Pagination `json:"pagination"`
				} `json:"meta"`
				Included []map[string]interface{} `json:"included"`
			}
			if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
				yield(schemas.WebhookIntegrationDelivery{}, fmt.Errorf("failed to decode response: %w", err))
				return
			}

			// Yield each item
			for i := range result.Data {
				// Populate included resources into relationships
				if len(result.Included) > 0 {
					result.Data[i].Relationships.PopulateIncludes(result.Included)
				}
				if !yield(result.Data[i], nil) {
					return // Consumer requested early exit
				}
			}

			// Check if there are more pages
			if result.Meta.Pagination == nil || result.Meta.Pagination.NextPage == nil {
				break
			}

			pageNum++
		}
	}
}

// ListWebhookIntegrationDeliveriesPaged returns a stateful iterator with access to pagination metadata.
// Use this when you need PageInfo(), Remaining(), or Collect() methods.
//
// Example:
//
//	iter := client.WebhookIntegrationDelivery.ListWebhookIntegrationDeliveriesPaged(ctx, opts)
//	for iter.Next() {
//	    item := iter.Value()
//	    fmt.Print(iter.PageInfo()) // -> "page 2/5"
//	    fmt.Printf("%d items remaining", iter.Remaining())
//	}
//	if err := iter.Err(); err != nil {
//	    // Handle error
//	}
func (c *Client) ListWebhookIntegrationDeliveriesPaged(ctx context.Context, opts *ListWebhookIntegrationDeliveriesOptions) *client.Iterator[schemas.WebhookIntegrationDelivery] {
	// Determine page size from opts or use default
	pageSize := 20
	if opts != nil && opts.PageSize > 0 {
		pageSize = opts.PageSize
	}

	// Fetch function that will be called for each page
	fetchPage := func(ctx context.Context, pageNum int) ([]*schemas.WebhookIntegrationDelivery, *client.Pagination, error) {
		// Copy options and set page number
		pageOpts := &ListWebhookIntegrationDeliveriesOptions{}
		if opts != nil {
			*pageOpts = *opts
		}
		pageOpts.PageNumber = pageNum
		pageOpts.PageSize = pageSize

		// Call the Raw method to get pagination metadata
		resp, err := c.ListWebhookIntegrationDeliveriesRaw(ctx, pageOpts)
		if err != nil {
			return nil, nil, err
		}
		defer resp.Body.Close()

		// Decode response
		var result struct {
			Data []schemas.WebhookIntegrationDelivery `json:"data"`
			Meta struct {
				Pagination *client.Pagination `json:"pagination"`
			} `json:"meta"`
			Included []map[string]interface{} `json:"included"`
		}
		if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
			return nil, nil, fmt.Errorf("failed to decode response: %w", err)
		}

		// Convert to slice of pointers and populate includes
		items := make([]*schemas.WebhookIntegrationDelivery, len(result.Data))
		for i := range result.Data {
			items[i] = &result.Data[i]
			// Populate included resources into relationships
			if len(result.Included) > 0 {
				items[i].Relationships.PopulateIncludes(result.Included)
			}
		}

		return items, result.Meta.Pagination, nil
	}

	return client.NewIterator[schemas.WebhookIntegrationDelivery](ctx, pageSize, fetchPage)
}

// ListWebhookIntegrationDeliveriesOptions holds optional parameters for ListWebhookIntegrationDeliveries
type ListWebhookIntegrationDeliveriesOptions struct {
	// Page number
	PageNumber int
	// Page size
	PageSize int
	// The comma-separated list of relationship paths.
	Include []string
	// The comma-separated list of attributes.
	Sort []string
	// Query string
	Query string
	// The value of the fields[resource-type] parameter is a comma-separated list that refers to the name of the fields to be returned for the resource. An empty value indicates that no fields should be returned.
	Fields map[string]interface{}
	Filter map[string]string
}
