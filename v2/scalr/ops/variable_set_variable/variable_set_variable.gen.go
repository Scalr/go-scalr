// Code generated by scalr-gen. DO NOT EDIT.

package variable_set_variable

import (
	"context"
	"encoding/json"
	"fmt"
	"iter"
	"net/url"
	"strings"

	"github.com/scalr/go-scalr/v2/scalr/client"
	"github.com/scalr/go-scalr/v2/scalr/schemas"
)

// Client provides access to VariableSetVariable operations
type Client struct {
	httpClient *client.HTTPClient
}

// New creates a new VariableSetVariable client
func New(httpClient *client.HTTPClient) *Client {
	return &Client{httpClient: httpClient}
}

// Creates a new variable set variable.
func (c *Client) CreateVarSetVariableRaw(ctx context.Context, req *schemas.VariableSetVariableRequest, opts *CreateVarSetVariableOptions) (*client.Response, error) {
	path := "/var-set-variables"

	params := url.Values{}
	if opts != nil {
		if len(opts.Include) > 0 {
			params.Set("include", strings.Join(opts.Include, ","))
		}
		// Add filters
		for k, v := range opts.Filter {
			params.Set("filter["+k+"]", v)
		}
	}
	if len(params) > 0 {
		path += "?" + params.Encode()
	}

	// Wrap request in JSON:API envelope
	body := map[string]interface{}{"data": req}
	httpResp, err := c.httpClient.Post(ctx, path, body, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// Creates a new variable set variable.
func (c *Client) CreateVarSetVariable(ctx context.Context, req *schemas.VariableSetVariableRequest, opts *CreateVarSetVariableOptions) (*schemas.VariableSetVariable, error) {
	resp, err := c.CreateVarSetVariableRaw(ctx, req, opts)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()

	var result struct {
		Data     schemas.VariableSetVariable `json:"data"`
		Included []map[string]interface{}    `json:"included"`
	}
	if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
		return nil, fmt.Errorf("failed to decode response: %w", err)
	}

	// Populate included resources into relationships
	if len(result.Included) > 0 {
		result.Data.Relationships.PopulateIncludes(result.Included)
	}
	return &result.Data, nil
}

// CreateVarSetVariableOptions holds optional parameters for CreateVarSetVariable
type CreateVarSetVariableOptions struct {
	// The comma-separated list of relationship paths.
	Include []string
	Filter  map[string]string
}

// Deletes a variable set variable.
func (c *Client) DeleteVarSetVariableRaw(ctx context.Context, vsVar string) (*client.Response, error) {
	path := "/var-set-variables/{vs_var}"
	path = strings.ReplaceAll(path, "{vs_var}", url.PathEscape(vsVar))

	httpResp, err := c.httpClient.Delete(ctx, path, nil, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// Deletes a variable set variable.
func (c *Client) DeleteVarSetVariable(ctx context.Context, vsVar string) error {
	resp, err := c.DeleteVarSetVariableRaw(ctx, vsVar)
	if err != nil {
		return err
	}
	defer resp.Body.Close()

	return nil
}

func (c *Client) GetVarSetVariableRaw(ctx context.Context, vsVar string, opts *GetVarSetVariableOptions) (*client.Response, error) {
	path := "/var-set-variables/{vs_var}"
	path = strings.ReplaceAll(path, "{vs_var}", url.PathEscape(vsVar))

	params := url.Values{}
	if opts != nil {
		if len(opts.Include) > 0 {
			params.Set("include", strings.Join(opts.Include, ","))
		}
		// Add filters
		for k, v := range opts.Filter {
			params.Set("filter["+k+"]", v)
		}
	}
	if len(params) > 0 {
		path += "?" + params.Encode()
	}

	httpResp, err := c.httpClient.Get(ctx, path, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

func (c *Client) GetVarSetVariable(ctx context.Context, vsVar string, opts *GetVarSetVariableOptions) (*schemas.VariableSetVariable, error) {
	resp, err := c.GetVarSetVariableRaw(ctx, vsVar, opts)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()

	var result struct {
		Data     schemas.VariableSetVariable `json:"data"`
		Included []map[string]interface{}    `json:"included"`
	}
	if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
		return nil, fmt.Errorf("failed to decode response: %w", err)
	}

	// Populate included resources into relationships
	if len(result.Included) > 0 {
		result.Data.Relationships.PopulateIncludes(result.Included)
	}
	return &result.Data, nil
}

// GetVarSetVariableOptions holds optional parameters for GetVarSetVariable
type GetVarSetVariableOptions struct {
	// The comma-separated list of relationship paths.
	Include []string
	Filter  map[string]string
}

// Returns a list of variable set variables.
func (c *Client) ListVarSetVariablesRaw(ctx context.Context, opts *ListVarSetVariablesOptions) (*client.Response, error) {
	path := "/var-set-variables"

	params := url.Values{}
	if opts != nil {
		if opts.PageNumber > 0 {
			params.Set("page[number]", fmt.Sprintf("%d", opts.PageNumber))
		}
		if opts.PageSize > 0 {
			params.Set("page[size]", fmt.Sprintf("%d", opts.PageSize))
		}
		if len(opts.Sort) > 0 {
			params.Set("sort", strings.Join(opts.Sort, ","))
		}
		if len(opts.Include) > 0 {
			params.Set("include", strings.Join(opts.Include, ","))
		}
		// Add filters
		for k, v := range opts.Filter {
			params.Set("filter["+k+"]", v)
		}
	}
	if len(params) > 0 {
		path += "?" + params.Encode()
	}

	httpResp, err := c.httpClient.Get(ctx, path, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// Returns a list of variable set variables.
func (c *Client) ListVarSetVariables(ctx context.Context, opts *ListVarSetVariablesOptions) ([]*schemas.VariableSetVariable, error) {
	resp, err := c.ListVarSetVariablesRaw(ctx, opts)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()

	var result struct {
		Data []schemas.VariableSetVariable `json:"data"`
		Meta struct {
			Pagination *client.Pagination `json:"pagination"`
		} `json:"meta"`
		Included []map[string]interface{} `json:"included"`
	}
	if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
		return nil, fmt.Errorf("failed to decode response: %w", err)
	}

	resources := make([]*schemas.VariableSetVariable, len(result.Data))
	for i := range result.Data {
		resources[i] = &result.Data[i]
		// Populate included resources into relationships
		if len(result.Included) > 0 {
			resources[i].Relationships.PopulateIncludes(result.Included)
		}
	}
	return resources, nil
}

// ListVarSetVariablesIter returns an iterator for paginated results using Go 1.23+ range over iter.Seq2 feature.
// This is the recommended and simple way to iterate through all the results.
//
// The iterator automatically fetches pages as needed and handles errors inline.
//
// Example:
//
//	for item, err := range client.VariableSetVariable.ListVarSetVariablesIter(ctx, opts) {
//	    if err != nil {
//	        return err
//	    }
//	    // Process item
//	}
func (c *Client) ListVarSetVariablesIter(ctx context.Context, opts *ListVarSetVariablesOptions) iter.Seq2[schemas.VariableSetVariable, error] {
	return func(yield func(schemas.VariableSetVariable, error) bool) {
		// Determine page size from opts or use default
		pageSize := 20
		if opts != nil && opts.PageSize > 0 {
			pageSize = opts.PageSize
		}

		pageNum := 1

		for {
			// Check context
			if err := ctx.Err(); err != nil {
				yield(schemas.VariableSetVariable{}, err)
				return
			}

			// Copy options and set page number
			pageOpts := &ListVarSetVariablesOptions{}
			if opts != nil {
				*pageOpts = *opts
			}
			pageOpts.PageNumber = pageNum
			pageOpts.PageSize = pageSize

			// Fetch page using Raw method to get pagination metadata
			resp, err := c.ListVarSetVariablesRaw(ctx, pageOpts)
			if err != nil {
				yield(schemas.VariableSetVariable{}, err)
				return
			}
			defer resp.Body.Close()

			// Decode response
			var result struct {
				Data []schemas.VariableSetVariable `json:"data"`
				Meta struct {
					Pagination *client.Pagination `json:"pagination"`
				} `json:"meta"`
				Included []map[string]interface{} `json:"included"`
			}
			if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
				yield(schemas.VariableSetVariable{}, fmt.Errorf("failed to decode response: %w", err))
				return
			}

			// Yield each item
			for i := range result.Data {
				// Populate included resources into relationships
				if len(result.Included) > 0 {
					result.Data[i].Relationships.PopulateIncludes(result.Included)
				}
				if !yield(result.Data[i], nil) {
					return // Consumer requested early exit
				}
			}

			// Check if there are more pages
			if result.Meta.Pagination == nil || result.Meta.Pagination.NextPage == nil {
				break
			}

			pageNum++
		}
	}
}

// ListVarSetVariablesPaged returns a stateful iterator with access to pagination metadata.
// Use this when you need PageInfo(), Remaining(), or Collect() methods.
//
// Example:
//
//	iter := client.VariableSetVariable.ListVarSetVariablesPaged(ctx, opts)
//	for iter.Next() {
//	    item := iter.Value()
//	    fmt.Print(iter.PageInfo()) // -> "page 2/5"
//	    fmt.Printf("%d items remaining", iter.Remaining())
//	}
//	if err := iter.Err(); err != nil {
//	    // Handle error
//	}
func (c *Client) ListVarSetVariablesPaged(ctx context.Context, opts *ListVarSetVariablesOptions) *client.Iterator[schemas.VariableSetVariable] {
	// Determine page size from opts or use default
	pageSize := 20
	if opts != nil && opts.PageSize > 0 {
		pageSize = opts.PageSize
	}

	// Fetch function that will be called for each page
	fetchPage := func(ctx context.Context, pageNum int) ([]*schemas.VariableSetVariable, *client.Pagination, error) {
		// Copy options and set page number
		pageOpts := &ListVarSetVariablesOptions{}
		if opts != nil {
			*pageOpts = *opts
		}
		pageOpts.PageNumber = pageNum
		pageOpts.PageSize = pageSize

		// Call the Raw method to get pagination metadata
		resp, err := c.ListVarSetVariablesRaw(ctx, pageOpts)
		if err != nil {
			return nil, nil, err
		}
		defer resp.Body.Close()

		// Decode response
		var result struct {
			Data []schemas.VariableSetVariable `json:"data"`
			Meta struct {
				Pagination *client.Pagination `json:"pagination"`
			} `json:"meta"`
			Included []map[string]interface{} `json:"included"`
		}
		if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
			return nil, nil, fmt.Errorf("failed to decode response: %w", err)
		}

		// Convert to slice of pointers and populate includes
		items := make([]*schemas.VariableSetVariable, len(result.Data))
		for i := range result.Data {
			items[i] = &result.Data[i]
			// Populate included resources into relationships
			if len(result.Included) > 0 {
				items[i].Relationships.PopulateIncludes(result.Included)
			}
		}

		return items, result.Meta.Pagination, nil
	}

	return client.NewIterator[schemas.VariableSetVariable](ctx, pageSize, fetchPage)
}

// ListVarSetVariablesOptions holds optional parameters for ListVarSetVariables
type ListVarSetVariablesOptions struct {
	// Page number.
	PageNumber int
	// Page size.
	PageSize int
	// The comma-separated list of attributes.
	Sort []string
	// The comma-separated list of relationship paths.
	Include []string
	Filter  map[string]string
}

// Updates attributes of an existing variable set variable.
func (c *Client) UpdateVarSetVariableRaw(ctx context.Context, vsVar string, req *schemas.VariableSetVariableRequest, opts *UpdateVarSetVariableOptions) (*client.Response, error) {
	path := "/var-set-variables/{vs_var}"
	path = strings.ReplaceAll(path, "{vs_var}", url.PathEscape(vsVar))

	params := url.Values{}
	if opts != nil {
		if len(opts.Include) > 0 {
			params.Set("include", strings.Join(opts.Include, ","))
		}
		// Add filters
		for k, v := range opts.Filter {
			params.Set("filter["+k+"]", v)
		}
	}
	if len(params) > 0 {
		path += "?" + params.Encode()
	}

	// Wrap request in JSON:API envelope
	body := map[string]interface{}{"data": req}
	httpResp, err := c.httpClient.Patch(ctx, path, body, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// Updates attributes of an existing variable set variable.
func (c *Client) UpdateVarSetVariable(ctx context.Context, vsVar string, req *schemas.VariableSetVariableRequest, opts *UpdateVarSetVariableOptions) (*schemas.VariableSetVariable, error) {
	resp, err := c.UpdateVarSetVariableRaw(ctx, vsVar, req, opts)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()

	var result struct {
		Data     schemas.VariableSetVariable `json:"data"`
		Included []map[string]interface{}    `json:"included"`
	}
	if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
		return nil, fmt.Errorf("failed to decode response: %w", err)
	}

	// Populate included resources into relationships
	if len(result.Included) > 0 {
		result.Data.Relationships.PopulateIncludes(result.Included)
	}
	return &result.Data, nil
}

// UpdateVarSetVariableOptions holds optional parameters for UpdateVarSetVariable
type UpdateVarSetVariableOptions struct {
	// The comma-separated list of relationship paths.
	Include []string
	Filter  map[string]string
}
