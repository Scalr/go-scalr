// Code generated by scalr-gen. DO NOT EDIT.

package gpg_key

import (
	"context"
	"encoding/json"
	"fmt"
	"iter"
	"net/url"
	"strings"

	"github.com/scalr/go-scalr/v2/scalr/client"
	"github.com/scalr/go-scalr/v2/scalr/schemas"
)

// Client provides access to GPGKey operations
type Client struct {
	httpClient *client.HTTPClient
}

// New creates a new GPGKey client
func New(httpClient *client.HTTPClient) *Client {
	return &Client{httpClient: httpClient}
}

// Create a new GPG key.
func (c *Client) CreateGpgKeyRaw(ctx context.Context, req *schemas.GPGKeyRequest) (*client.Response, error) {
	path := "/gpg-keys"

	// Wrap request in JSON:API envelope
	body := map[string]interface{}{"data": req}
	httpResp, err := c.httpClient.Post(ctx, path, body, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// Create a new GPG key.
func (c *Client) CreateGpgKey(ctx context.Context, req *schemas.GPGKeyRequest) (*schemas.GPGKey, error) {
	resp, err := c.CreateGpgKeyRaw(ctx, req)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()

	var result struct {
		Data     schemas.GPGKey           `json:"data"`
		Included []map[string]interface{} `json:"included"`
	}
	if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
		return nil, fmt.Errorf("failed to decode response: %w", err)
	}

	return &result.Data, nil
}

// The endpoint deletes a GPG key by ID.
func (c *Client) DeleteGpgKeyRaw(ctx context.Context, gpgKey string) (*client.Response, error) {
	path := "/gpg-keys/{gpg_key}"
	path = strings.ReplaceAll(path, "{gpg_key}", url.PathEscape(gpgKey))

	httpResp, err := c.httpClient.Delete(ctx, path, nil, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// The endpoint deletes a GPG key by ID.
func (c *Client) DeleteGpgKey(ctx context.Context, gpgKey string) error {
	resp, err := c.DeleteGpgKeyRaw(ctx, gpgKey)
	if err != nil {
		return err
	}
	defer resp.Body.Close()

	return nil
}

// Show details of a specific GPG key.
func (c *Client) GetGpgKeyRaw(ctx context.Context, gpgKey string, opts *GetGpgKeyOptions) (*client.Response, error) {
	path := "/gpg-keys/{gpg_key}"
	path = strings.ReplaceAll(path, "{gpg_key}", url.PathEscape(gpgKey))

	params := url.Values{}
	if opts != nil {
		// Handle parameter: Fields (map[string]interface{})
		// Complex type map[string]interface{} - skip for now
		// Add filters
		for k, v := range opts.Filter {
			params.Set("filter["+k+"]", v)
		}
	}
	if len(params) > 0 {
		path += "?" + params.Encode()
	}

	httpResp, err := c.httpClient.Get(ctx, path, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// Show details of a specific GPG key.
func (c *Client) GetGpgKey(ctx context.Context, gpgKey string, opts *GetGpgKeyOptions) (*schemas.GPGKey, error) {
	resp, err := c.GetGpgKeyRaw(ctx, gpgKey, opts)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()

	var result struct {
		Data     schemas.GPGKey           `json:"data"`
		Included []map[string]interface{} `json:"included"`
	}
	if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
		return nil, fmt.Errorf("failed to decode response: %w", err)
	}

	return &result.Data, nil
}

// GetGpgKeyOptions holds optional parameters for GetGpgKey
type GetGpgKeyOptions struct {
	// The value of the fields[resource-type] parameter is a comma-separated list that refers to the name of the fields to be returned for the resource. An empty value indicates that no fields should be returned.
	Fields map[string]interface{}
	Filter map[string]string
}

// This endpoint returns a list of GPG keys.
func (c *Client) ListGpgKeysRaw(ctx context.Context, opts *ListGpgKeysOptions) (*client.Response, error) {
	path := "/gpg-keys"

	params := url.Values{}
	if opts != nil {
		// Handle parameter: Query (string)
		if opts.Query != "" {
			params.Set("query", opts.Query)
		}
		if opts.PageNumber > 0 {
			params.Set("page[number]", fmt.Sprintf("%d", opts.PageNumber))
		}
		if opts.PageSize > 0 {
			params.Set("page[size]", fmt.Sprintf("%d", opts.PageSize))
		}
		if len(opts.Sort) > 0 {
			params.Set("sort", strings.Join(opts.Sort, ","))
		}
		// Handle parameter: Fields (map[string]interface{})
		// Complex type map[string]interface{} - skip for now
		// Add filters
		for k, v := range opts.Filter {
			params.Set("filter["+k+"]", v)
		}
	}
	if len(params) > 0 {
		path += "?" + params.Encode()
	}

	httpResp, err := c.httpClient.Get(ctx, path, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// This endpoint returns a list of GPG keys.
func (c *Client) ListGpgKeys(ctx context.Context, opts *ListGpgKeysOptions) ([]*schemas.GPGKey, error) {
	resp, err := c.ListGpgKeysRaw(ctx, opts)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()

	var result struct {
		Data []schemas.GPGKey `json:"data"`
		Meta struct {
			Pagination *client.Pagination `json:"pagination"`
		} `json:"meta"`
		Included []map[string]interface{} `json:"included"`
	}
	if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
		return nil, fmt.Errorf("failed to decode response: %w", err)
	}

	resources := make([]*schemas.GPGKey, len(result.Data))
	for i := range result.Data {
		resources[i] = &result.Data[i]
	}
	return resources, nil
}

// ListGpgKeysIter returns an iterator for paginated results using Go 1.23+ range over iter.Seq2 feature.
// This is the recommended and simple way to iterate through all the results.
//
// The iterator automatically fetches pages as needed and handles errors inline.
//
// Example:
//
//	for item, err := range client.GPGKey.ListGpgKeysIter(ctx, opts) {
//	    if err != nil {
//	        return err
//	    }
//	    // Process item
//	}
func (c *Client) ListGpgKeysIter(ctx context.Context, opts *ListGpgKeysOptions) iter.Seq2[schemas.GPGKey, error] {
	return func(yield func(schemas.GPGKey, error) bool) {
		// Determine page size from opts or use default
		pageSize := 20
		if opts != nil && opts.PageSize > 0 {
			pageSize = opts.PageSize
		}

		pageNum := 1

		for {
			// Check context
			if err := ctx.Err(); err != nil {
				yield(schemas.GPGKey{}, err)
				return
			}

			// Copy options and set page number
			pageOpts := &ListGpgKeysOptions{}
			if opts != nil {
				*pageOpts = *opts
			}
			pageOpts.PageNumber = pageNum
			pageOpts.PageSize = pageSize

			// Fetch page using Raw method to get pagination metadata
			resp, err := c.ListGpgKeysRaw(ctx, pageOpts)
			if err != nil {
				yield(schemas.GPGKey{}, err)
				return
			}
			defer resp.Body.Close()

			// Decode response
			var result struct {
				Data []schemas.GPGKey `json:"data"`
				Meta struct {
					Pagination *client.Pagination `json:"pagination"`
				} `json:"meta"`
				Included []map[string]interface{} `json:"included"`
			}
			if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
				yield(schemas.GPGKey{}, fmt.Errorf("failed to decode response: %w", err))
				return
			}

			// Yield each item
			for i := range result.Data {
				if !yield(result.Data[i], nil) {
					return // Consumer requested early exit
				}
			}

			// Check if there are more pages
			if result.Meta.Pagination == nil || result.Meta.Pagination.NextPage == nil {
				break
			}

			pageNum++
		}
	}
}

// ListGpgKeysPaged returns a stateful iterator with access to pagination metadata.
// Use this when you need PageInfo(), Remaining(), or Collect() methods.
//
// Example:
//
//	iter := client.GPGKey.ListGpgKeysPaged(ctx, opts)
//	for iter.Next() {
//	    item := iter.Value()
//	    fmt.Print(iter.PageInfo()) // -> "page 2/5"
//	    fmt.Printf("%d items remaining", iter.Remaining())
//	}
//	if err := iter.Err(); err != nil {
//	    // Handle error
//	}
func (c *Client) ListGpgKeysPaged(ctx context.Context, opts *ListGpgKeysOptions) *client.Iterator[schemas.GPGKey] {
	// Determine page size from opts or use default
	pageSize := 20
	if opts != nil && opts.PageSize > 0 {
		pageSize = opts.PageSize
	}

	// Fetch function that will be called for each page
	fetchPage := func(ctx context.Context, pageNum int) ([]*schemas.GPGKey, *client.Pagination, error) {
		// Copy options and set page number
		pageOpts := &ListGpgKeysOptions{}
		if opts != nil {
			*pageOpts = *opts
		}
		pageOpts.PageNumber = pageNum
		pageOpts.PageSize = pageSize

		// Call the Raw method to get pagination metadata
		resp, err := c.ListGpgKeysRaw(ctx, pageOpts)
		if err != nil {
			return nil, nil, err
		}
		defer resp.Body.Close()

		// Decode response
		var result struct {
			Data []schemas.GPGKey `json:"data"`
			Meta struct {
				Pagination *client.Pagination `json:"pagination"`
			} `json:"meta"`
			Included []map[string]interface{} `json:"included"`
		}
		if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
			return nil, nil, fmt.Errorf("failed to decode response: %w", err)
		}

		// Convert to slice of pointers and populate includes
		items := make([]*schemas.GPGKey, len(result.Data))
		for i := range result.Data {
			items[i] = &result.Data[i]
		}

		return items, result.Meta.Pagination, nil
	}

	return client.NewIterator[schemas.GPGKey](ctx, pageSize, fetchPage)
}

// ListGpgKeysOptions holds optional parameters for ListGpgKeys
type ListGpgKeysOptions struct {
	// The search string. Supports searching by GPG key name and id.
	Query string
	// Page number
	PageNumber int
	// Page size
	PageSize int
	// The comma-separated list of attributes.
	Sort []string
	// The value of the fields[resource-type] parameter is a comma-separated list that refers to the name of the fields to be returned for the resource. An empty value indicates that no fields should be returned.
	Fields map[string]interface{}
	Filter map[string]string
}

// This endpoint updates a GPG key.
func (c *Client) UpdateGpgKeyRaw(ctx context.Context, gpgKey string, req *schemas.GPGKeyRequest) (*client.Response, error) {
	path := "/gpg-keys/{gpg_key}"
	path = strings.ReplaceAll(path, "{gpg_key}", url.PathEscape(gpgKey))

	// Wrap request in JSON:API envelope
	body := map[string]interface{}{"data": req}
	httpResp, err := c.httpClient.Patch(ctx, path, body, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// This endpoint updates a GPG key.
func (c *Client) UpdateGpgKey(ctx context.Context, gpgKey string, req *schemas.GPGKeyRequest) (*schemas.GPGKey, error) {
	resp, err := c.UpdateGpgKeyRaw(ctx, gpgKey, req)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()

	var result struct {
		Data     schemas.GPGKey           `json:"data"`
		Included []map[string]interface{} `json:"included"`
	}
	if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
		return nil, fmt.Errorf("failed to decode response: %w", err)
	}

	return &result.Data, nil
}
