// Code generated by scalr-gen. DO NOT EDIT.

package docker_integration

import (
	"context"
	"encoding/json"
	"fmt"
	"iter"
	"net/url"
	"strings"

	"github.com/scalr/go-scalr/v2/scalr/client"
	"github.com/scalr/go-scalr/v2/scalr/schemas"
)

// Client provides access to DockerIntegration operations
type Client struct {
	httpClient *client.HTTPClient
}

// New creates a new DockerIntegration client
func New(httpClient *client.HTTPClient) *Client {
	return &Client{httpClient: httpClient}
}

// Create a Docker integration.
func (c *Client) CreateDockerIntegrationRaw(ctx context.Context, req *schemas.DockerIntegrationRequest) (*client.Response, error) {
	path := "/integrations/docker"

	// Wrap request in JSON:API envelope
	body := map[string]interface{}{"data": req}
	httpResp, err := c.httpClient.Post(ctx, path, body, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// Create a Docker integration.
func (c *Client) CreateDockerIntegration(ctx context.Context, req *schemas.DockerIntegrationRequest) (*schemas.DockerIntegration, error) {
	resp, err := c.CreateDockerIntegrationRaw(ctx, req)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()

	var result struct {
		Data     schemas.DockerIntegration `json:"data"`
		Included []map[string]interface{}  `json:"included"`
	}
	if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
		return nil, fmt.Errorf("failed to decode response: %w", err)
	}

	// Populate included resources into relationships
	if len(result.Included) > 0 {
		result.Data.Relationships.PopulateIncludes(result.Included)
	}
	return &result.Data, nil
}

// Delete a Docker integration.
func (c *Client) DeleteDockerIntegrationRaw(ctx context.Context, dockerIntegration string) (*client.Response, error) {
	path := "/integrations/docker/{docker_integration}"
	if dockerIntegration == "" {
		return nil, fmt.Errorf("dockerIntegration must not be empty")
	}
	path = strings.ReplaceAll(path, "{docker_integration}", url.PathEscape(dockerIntegration))

	httpResp, err := c.httpClient.Delete(ctx, path, nil, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// Delete a Docker integration.
func (c *Client) DeleteDockerIntegration(ctx context.Context, dockerIntegration string) error {
	resp, err := c.DeleteDockerIntegrationRaw(ctx, dockerIntegration)
	if err != nil {
		return err
	}
	defer resp.Body.Close()

	return nil
}

// Get a Docker integration.
func (c *Client) GetDockerIntegrationRaw(ctx context.Context, dockerIntegration string, opts *GetDockerIntegrationOptions) (*client.Response, error) {
	path := "/integrations/docker/{docker_integration}"
	if dockerIntegration == "" {
		return nil, fmt.Errorf("dockerIntegration must not be empty")
	}
	path = strings.ReplaceAll(path, "{docker_integration}", url.PathEscape(dockerIntegration))

	params := url.Values{}
	if opts != nil {
		// Sparse fieldsets
		for resourceType, fields := range opts.Fields {
			params.Set("fields["+resourceType+"]", fields)
		}
		// Add filters (keys should be full parameter names like "filter[account]")
		for k, v := range opts.Filters {
			params.Set(k, v)
		}
	}
	if len(params) > 0 {
		path += "?" + params.Encode()
	}

	httpResp, err := c.httpClient.Get(ctx, path, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// Get a Docker integration.
func (c *Client) GetDockerIntegration(ctx context.Context, dockerIntegration string, opts *GetDockerIntegrationOptions) (*schemas.DockerIntegration, error) {
	resp, err := c.GetDockerIntegrationRaw(ctx, dockerIntegration, opts)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()

	var result struct {
		Data     schemas.DockerIntegration `json:"data"`
		Included []map[string]interface{}  `json:"included"`
	}
	if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
		return nil, fmt.Errorf("failed to decode response: %w", err)
	}

	// Populate included resources into relationships
	if len(result.Included) > 0 {
		result.Data.Relationships.PopulateIncludes(result.Included)
	}
	return &result.Data, nil
}

// GetDockerIntegrationOptions holds optional parameters for GetDockerIntegration
type GetDockerIntegrationOptions struct {
	// Fields specifies which attributes to return for each resource type.
	Fields map[string]string
	// Filters maps filter keys to their values.
	// Use the Filter* constants defined in this package.
	Filters map[string]string
}

// List Docker integrations.
func (c *Client) ListDockerIntegrationsRaw(ctx context.Context, opts *ListDockerIntegrationsOptions) (*client.Response, error) {
	path := "/integrations/docker"

	params := url.Values{}
	if opts != nil {
		if opts.PageNumber > 0 {
			params.Set("page[number]", fmt.Sprintf("%d", opts.PageNumber))
		}
		if opts.PageSize > 0 {
			params.Set("page[size]", fmt.Sprintf("%d", opts.PageSize))
		}
		if len(opts.Sort) > 0 {
			params.Set("sort", strings.Join(opts.Sort, ","))
		}
		// Sparse fieldsets
		for resourceType, fields := range opts.Fields {
			params.Set("fields["+resourceType+"]", fields)
		}
		// Add filters (keys should be full parameter names like "filter[account]")
		for k, v := range opts.Filters {
			params.Set(k, v)
		}
	}
	if len(params) > 0 {
		path += "?" + params.Encode()
	}

	httpResp, err := c.httpClient.Get(ctx, path, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// List Docker integrations.
func (c *Client) ListDockerIntegrations(ctx context.Context, opts *ListDockerIntegrationsOptions) ([]*schemas.DockerIntegration, error) {
	resp, err := c.ListDockerIntegrationsRaw(ctx, opts)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()

	var result struct {
		Data []schemas.DockerIntegration `json:"data"`
		Meta struct {
			Pagination *client.Pagination `json:"pagination"`
		} `json:"meta"`
		Included []map[string]interface{} `json:"included"`
	}
	if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
		return nil, fmt.Errorf("failed to decode response: %w", err)
	}

	resources := make([]*schemas.DockerIntegration, len(result.Data))
	for i := range result.Data {
		resources[i] = &result.Data[i]
		// Populate included resources into relationships
		if len(result.Included) > 0 {
			resources[i].Relationships.PopulateIncludes(result.Included)
		}
	}
	return resources, nil
}

// ListDockerIntegrationsIter returns an iterator for paginated results using Go 1.23+ range over iter.Seq2 feature.
// This is the recommended and simple way to iterate through all the results.
//
// The iterator automatically fetches pages as needed and handles errors inline.
//
// Example:
//
//	for item, err := range client.DockerIntegration.ListDockerIntegrationsIter(ctx, opts) {
//	    if err != nil {
//	        return err
//	    }
//	    // Process item
//	}
func (c *Client) ListDockerIntegrationsIter(ctx context.Context, opts *ListDockerIntegrationsOptions) iter.Seq2[schemas.DockerIntegration, error] {
	return func(yield func(schemas.DockerIntegration, error) bool) {
		// Determine page size from opts or use default
		pageSize := 20
		if opts != nil && opts.PageSize > 0 {
			pageSize = opts.PageSize
		}

		pageNum := 1

		for {
			// Check context
			if err := ctx.Err(); err != nil {
				yield(schemas.DockerIntegration{}, err)
				return
			}

			// Copy options and set page number
			pageOpts := &ListDockerIntegrationsOptions{}
			if opts != nil {
				*pageOpts = *opts
			}
			pageOpts.PageNumber = pageNum
			pageOpts.PageSize = pageSize

			// Fetch page using Raw method to get pagination metadata
			resp, err := c.ListDockerIntegrationsRaw(ctx, pageOpts)
			if err != nil {
				yield(schemas.DockerIntegration{}, err)
				return
			}

			// Decode response
			var result struct {
				Data []schemas.DockerIntegration `json:"data"`
				Meta struct {
					Pagination *client.Pagination `json:"pagination"`
				} `json:"meta"`
				Included []map[string]interface{} `json:"included"`
			}
			decodeErr := json.NewDecoder(resp.Body).Decode(&result)
			resp.Body.Close()
			if decodeErr != nil {
				yield(schemas.DockerIntegration{}, fmt.Errorf("failed to decode response: %w", decodeErr))
				return
			}

			// Yield each item
			for i := range result.Data {
				// Populate included resources into relationships
				if len(result.Included) > 0 {
					result.Data[i].Relationships.PopulateIncludes(result.Included)
				}
				if !yield(result.Data[i], nil) {
					return // Consumer requested early exit
				}
			}

			// Check if there are more pages
			if result.Meta.Pagination == nil || result.Meta.Pagination.NextPage == nil {
				break
			}

			pageNum++
		}
	}
}

// ListDockerIntegrationsPaged returns a stateful iterator with access to pagination metadata.
// Use this when you need PageInfo(), Remaining(), or Collect() methods.
//
// Example:
//
//	iter := client.DockerIntegration.ListDockerIntegrationsPaged(ctx, opts)
//	for iter.Next() {
//	    item := iter.Value()
//	    fmt.Print(iter.PageInfo()) // -> "page 2/5"
//	    fmt.Printf("%d items remaining", iter.Remaining())
//	}
//	if err := iter.Err(); err != nil {
//	    // Handle error
//	}
func (c *Client) ListDockerIntegrationsPaged(ctx context.Context, opts *ListDockerIntegrationsOptions) *client.Iterator[schemas.DockerIntegration] {
	// Determine page size from opts or use default
	pageSize := 20
	if opts != nil && opts.PageSize > 0 {
		pageSize = opts.PageSize
	}

	// Fetch function that will be called for each page
	fetchPage := func(ctx context.Context, pageNum int) ([]*schemas.DockerIntegration, *client.Pagination, error) {
		// Copy options and set page number
		pageOpts := &ListDockerIntegrationsOptions{}
		if opts != nil {
			*pageOpts = *opts
		}
		pageOpts.PageNumber = pageNum
		pageOpts.PageSize = pageSize

		// Call the Raw method to get pagination metadata
		resp, err := c.ListDockerIntegrationsRaw(ctx, pageOpts)
		if err != nil {
			return nil, nil, err
		}
		defer resp.Body.Close()

		// Decode response
		var result struct {
			Data []schemas.DockerIntegration `json:"data"`
			Meta struct {
				Pagination *client.Pagination `json:"pagination"`
			} `json:"meta"`
			Included []map[string]interface{} `json:"included"`
		}
		if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
			return nil, nil, fmt.Errorf("failed to decode response: %w", err)
		}

		// Convert to slice of pointers and populate includes
		items := make([]*schemas.DockerIntegration, len(result.Data))
		for i := range result.Data {
			items[i] = &result.Data[i]
			// Populate included resources into relationships
			if len(result.Included) > 0 {
				items[i].Relationships.PopulateIncludes(result.Included)
			}
		}

		return items, result.Meta.Pagination, nil
	}

	return client.NewIterator[schemas.DockerIntegration](ctx, pageSize, fetchPage)
}

// ListDockerIntegrationsOptions holds optional parameters for ListDockerIntegrations
type ListDockerIntegrationsOptions struct {
	// Page number
	PageNumber int
	// Page size
	PageSize int
	// The comma-separated list of attributes.
	Sort []string
	// Fields specifies which attributes to return for each resource type.
	Fields map[string]string
	// Filters maps filter keys to their values.
	// Use the Filter* constants defined in this package.
	Filters map[string]string
}

// Update a Docker integration.
func (c *Client) UpdateDockerIntegrationRaw(ctx context.Context, dockerIntegration string, req *schemas.DockerIntegrationRequest) (*client.Response, error) {
	path := "/integrations/docker/{docker_integration}"
	if dockerIntegration == "" {
		return nil, fmt.Errorf("dockerIntegration must not be empty")
	}
	path = strings.ReplaceAll(path, "{docker_integration}", url.PathEscape(dockerIntegration))

	// Wrap request in JSON:API envelope
	body := map[string]interface{}{"data": req}
	httpResp, err := c.httpClient.Patch(ctx, path, body, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// Update a Docker integration.
func (c *Client) UpdateDockerIntegration(ctx context.Context, dockerIntegration string, req *schemas.DockerIntegrationRequest) (*schemas.DockerIntegration, error) {
	resp, err := c.UpdateDockerIntegrationRaw(ctx, dockerIntegration, req)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()

	var result struct {
		Data     schemas.DockerIntegration `json:"data"`
		Included []map[string]interface{}  `json:"included"`
	}
	if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
		return nil, fmt.Errorf("failed to decode response: %w", err)
	}

	// Populate included resources into relationships
	if len(result.Included) > 0 {
		result.Data.Relationships.PopulateIncludes(result.Included)
	}
	return &result.Data, nil
}
