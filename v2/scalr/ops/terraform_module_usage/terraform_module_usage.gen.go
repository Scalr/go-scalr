// Code generated by scalr-gen. DO NOT EDIT.

package terraform_module_usage

import (
	"context"
	"encoding/json"
	"fmt"
	"io"
	"iter"
	"net/http"
	"net/url"
	"strings"

	"github.com/scalr/go-scalr/v2/scalr/client"
	"github.com/scalr/go-scalr/v2/scalr/schemas"
)

// Client provides access to TerraformModuleUsage operations
type Client struct {
	httpClient *client.HTTPClient
}

// New creates a new TerraformModuleUsage client
func New(httpClient *client.HTTPClient) *Client {
	return &Client{httpClient: httpClient}
}

// This endpoint returns instance of module usage.
func (c *Client) GetModuleUsageRaw(ctx context.Context, moduleUsage string, opts *GetModuleUsageOptions) (*http.Response, error) {
	path := "/reports/modules/{module_usage}"
	path = strings.ReplaceAll(path, "{module_usage}", url.PathEscape(moduleUsage))

	params := url.Values{}
	if opts != nil {
		if len(opts.Include) > 0 {
			params.Set("include", strings.Join(opts.Include, ","))
		}
		// Handle parameter: Fields (map[string]interface{})
		// Complex type map[string]interface{} - skip for now
		// Add filters
		for k, v := range opts.Filter {
			params.Set("filter["+k+"]", v)
		}
	}
	if len(params) > 0 {
		path += "?" + params.Encode()
	}

	return c.httpClient.Get(ctx, path, nil)
}

// This endpoint returns instance of module usage.
func (c *Client) GetModuleUsage(ctx context.Context, moduleUsage string, opts *GetModuleUsageOptions) (*schemas.TerraformModuleUsage, *client.Response, error) {
	httpResp, err := c.GetModuleUsageRaw(ctx, moduleUsage, opts)
	if err != nil {
		return nil, nil, err
	}
	defer httpResp.Body.Close()

	resp := &client.Response{Response: httpResp}

	var result struct {
		Data     schemas.TerraformModuleUsage `json:"data"`
		Included []map[string]interface{}     `json:"included"`
	}
	if err := json.NewDecoder(httpResp.Body).Decode(&result); err != nil {
		return nil, resp, fmt.Errorf("failed to decode response: %w", err)
	}

	// Populate included resources into relationships
	if len(result.Included) > 0 {
		result.Data.Relationships.PopulateIncludes(result.Included)
	}
	return &result.Data, resp, nil
}

// GetModuleUsageOptions holds optional parameters for GetModuleUsage
type GetModuleUsageOptions struct {
	// The comma-separated list of relationship paths.
	Include []string
	// The value of the fields[resource-type] parameter is a comma-separated list that refers to the name of the fields to be returned for the resource. An empty value indicates that no fields should be returned.
	Fields map[string]interface{}
	Filter map[string]string
}

// This endpoint lists unique terraform module sources.
func (c *Client) ListTerraformModuleSourcesRaw(ctx context.Context, opts *ListTerraformModuleSourcesOptions) (*http.Response, error) {
	path := "/reports/module-sources"

	params := url.Values{}
	if opts != nil {
		// Handle parameter: Query (string)
		if opts.Query != "" {
			params.Set("query", opts.Query)
		}
		if opts.PageNumber > 0 {
			params.Set("page[number]", fmt.Sprintf("%d", opts.PageNumber))
		}
		if opts.PageSize > 0 {
			params.Set("page[size]", fmt.Sprintf("%d", opts.PageSize))
		}
		// Add filters
		for k, v := range opts.Filter {
			params.Set("filter["+k+"]", v)
		}
	}
	if len(params) > 0 {
		path += "?" + params.Encode()
	}

	return c.httpClient.Get(ctx, path, nil)
}

// This endpoint lists unique terraform module sources.
func (c *Client) ListTerraformModuleSources(ctx context.Context, opts *ListTerraformModuleSourcesOptions) (string, *client.Response, error) {
	httpResp, err := c.ListTerraformModuleSourcesRaw(ctx, opts)
	if err != nil {
		return "", nil, err
	}
	defer httpResp.Body.Close()

	resp := &client.Response{Response: httpResp}

	bodyBytes, err := io.ReadAll(httpResp.Body)
	if err != nil {
		return "", resp, fmt.Errorf("failed to read response body: %w", err)
	}
	return string(bodyBytes), resp, nil
}

// ListTerraformModuleSourcesOptions holds optional parameters for ListTerraformModuleSources
type ListTerraformModuleSourcesOptions struct {
	// The search string. Supports search by module source.
	Query string
	// Page number.
	PageNumber int
	// Page size.
	PageSize int
	Filter   map[string]string
}

// This endpoint lists terraform module usages.
func (c *Client) ListTerraformModuleUsagesRaw(ctx context.Context, opts *ListTerraformModuleUsagesOptions) (*http.Response, error) {
	path := "/reports/modules"

	params := url.Values{}
	if opts != nil {
		// Handle parameter: Query (string)
		if opts.Query != "" {
			params.Set("query", opts.Query)
		}
		if opts.PageNumber > 0 {
			params.Set("page[number]", fmt.Sprintf("%d", opts.PageNumber))
		}
		if opts.PageSize > 0 {
			params.Set("page[size]", fmt.Sprintf("%d", opts.PageSize))
		}
		// Handle parameter: Format (string)
		if opts.Format != "" {
			params.Set("format", opts.Format)
		}
		if len(opts.Sort) > 0 {
			params.Set("sort", strings.Join(opts.Sort, ","))
		}
		// Handle parameter: Fields (map[string]interface{})
		// Complex type map[string]interface{} - skip for now
		if len(opts.Include) > 0 {
			params.Set("include", strings.Join(opts.Include, ","))
		}
		// Add filters
		for k, v := range opts.Filter {
			params.Set("filter["+k+"]", v)
		}
	}
	if len(params) > 0 {
		path += "?" + params.Encode()
	}

	return c.httpClient.Get(ctx, path, nil)
}

// This endpoint lists terraform module usages.
func (c *Client) ListTerraformModuleUsages(ctx context.Context, opts *ListTerraformModuleUsagesOptions) ([]*schemas.TerraformModuleUsage, *client.Response, error) {
	httpResp, err := c.ListTerraformModuleUsagesRaw(ctx, opts)
	if err != nil {
		return nil, nil, err
	}
	defer httpResp.Body.Close()

	resp := &client.Response{Response: httpResp}

	var result struct {
		Data []schemas.TerraformModuleUsage `json:"data"`
		Meta struct {
			Pagination *client.Pagination `json:"pagination"`
		} `json:"meta"`
		Included []map[string]interface{} `json:"included"`
	}
	if err := json.NewDecoder(httpResp.Body).Decode(&result); err != nil {
		return nil, resp, fmt.Errorf("failed to decode response: %w", err)
	}

	resources := make([]*schemas.TerraformModuleUsage, len(result.Data))
	for i := range result.Data {
		resources[i] = &result.Data[i]
		// Populate included resources into relationships
		if len(result.Included) > 0 {
			resources[i].Relationships.PopulateIncludes(result.Included)
		}
	}
	resp.Pagination = result.Meta.Pagination
	return resources, resp, nil
}

// ListTerraformModuleUsagesIter returns an iterator for paginated results using Go 1.23+ range over iter.Seq2 feature.
// This is the recommended and simple way to iterate through all the results.
//
// The iterator automatically fetches pages as needed and handles errors inline.
//
// Example:
//
//	for item, err := range client.TerraformModuleUsage.ListTerraformModuleUsagesIter(ctx, opts) {
//	    if err != nil {
//	        return err
//	    }
//	    // Process item
//	}
func (c *Client) ListTerraformModuleUsagesIter(ctx context.Context, opts *ListTerraformModuleUsagesOptions) iter.Seq2[schemas.TerraformModuleUsage, error] {
	return func(yield func(schemas.TerraformModuleUsage, error) bool) {
		// Determine page size from opts or use default
		pageSize := 20
		if opts != nil && opts.PageSize > 0 {
			pageSize = opts.PageSize
		}

		pageNum := 1

		for {
			// Check context
			if err := ctx.Err(); err != nil {
				yield(schemas.TerraformModuleUsage{}, err)
				return
			}

			// Copy options and set page number
			pageOpts := &ListTerraformModuleUsagesOptions{}
			if opts != nil {
				*pageOpts = *opts
			}
			pageOpts.PageNumber = pageNum
			pageOpts.PageSize = pageSize

			// Fetch page
			items, resp, err := c.ListTerraformModuleUsages(ctx, pageOpts)
			if err != nil {
				yield(schemas.TerraformModuleUsage{}, err)
				return
			}

			// Yield each item
			for _, item := range items {
				if !yield(*item, nil) {
					return // Consumer requested early exit
				}
			}

			// Check if there are more pages
			if resp.Pagination == nil || resp.Pagination.NextPage == nil {
				break
			}

			pageNum++
		}
	}
}

// ListTerraformModuleUsagesPaged returns a stateful iterator with access to pagination metadata.
// Use this when you need PageInfo(), Remaining(), or Collect() methods.
//
// Example:
//
//	iter := client.TerraformModuleUsage.ListTerraformModuleUsagesPaged(ctx, opts)
//	for iter.Next() {
//	    item := iter.Value()
//	    fmt.Print(iter.PageInfo()) // -> "page 2/5"
//	    fmt.Printf("%d items remaining", iter.Remaining())
//	}
//	if err := iter.Err(); err != nil {
//	    // Handle error
//	}
func (c *Client) ListTerraformModuleUsagesPaged(ctx context.Context, opts *ListTerraformModuleUsagesOptions) *client.Iterator[schemas.TerraformModuleUsage] {
	// Determine page size from opts or use default
	pageSize := 20
	if opts != nil && opts.PageSize > 0 {
		pageSize = opts.PageSize
	}

	// Fetch function that will be called for each page
	fetchPage := func(ctx context.Context, pageNum int) ([]*schemas.TerraformModuleUsage, *client.Pagination, error) {
		// Copy options and set page number
		pageOpts := &ListTerraformModuleUsagesOptions{}
		if opts != nil {
			*pageOpts = *opts
		}
		pageOpts.PageNumber = pageNum
		pageOpts.PageSize = pageSize

		// Call the actual list method
		items, resp, err := c.ListTerraformModuleUsages(ctx, pageOpts)
		if err != nil {
			return nil, nil, err
		}

		return items, resp.Pagination, nil
	}

	return client.NewIterator[schemas.TerraformModuleUsage](ctx, pageSize, fetchPage)
}

// ListTerraformModuleUsagesOptions holds optional parameters for ListTerraformModuleUsages
type ListTerraformModuleUsagesOptions struct {
	// The search string. Supports search by module name or parent module name.
	Query string
	// Page number.
	PageNumber int
	// Page size.
	PageSize int
	// Format of the response. It can be 'json' or 'csv'.
	Format string
	// The comma-separated list of attributes.
	Sort []string
	// The value of the fields[resource-type] parameter is a comma-separated list that refers to the name of the fields to be returned for the resource. An empty value indicates that no fields should be returned.
	Fields map[string]interface{}
	// The comma-separated list of relationship paths.
	Include []string
	Filter  map[string]string
}
