// Code generated by scalr-gen. DO NOT EDIT.

package access_token_usage

import (
	"context"
	"encoding/json"
	"fmt"
	"iter"
	"net/http"
	"net/url"
	"strings"

	"github.com/scalr/go-scalr/v2/scalr/client"
	"github.com/scalr/go-scalr/v2/scalr/schemas"
)

// Client provides access to AccessTokenUsage operations
type Client struct {
	httpClient *client.HTTPClient
}

// New creates a new AccessTokenUsage client
func New(httpClient *client.HTTPClient) *Client {
	return &Client{httpClient: httpClient}
}

// This endpoint returns a list of access token usage on the account.
func (c *Client) ListAccessTokenUsageRaw(ctx context.Context, opts *ListAccessTokenUsageOptions) (*http.Response, error) {
	path := "/reports/access-tokens"

	params := url.Values{}
	if opts != nil {
		// Handle parameter: Format (string)
		if opts.Format != "" {
			params.Set("format", opts.Format)
		}
		if opts.PageNumber > 0 {
			params.Set("page[number]", fmt.Sprintf("%d", opts.PageNumber))
		}
		if opts.PageSize > 0 {
			params.Set("page[size]", fmt.Sprintf("%d", opts.PageSize))
		}
		// Handle parameter: Query (string)
		if opts.Query != "" {
			params.Set("query", opts.Query)
		}
		if len(opts.Sort) > 0 {
			params.Set("sort", strings.Join(opts.Sort, ","))
		}
		// Add filters
		for k, v := range opts.Filter {
			params.Set("filter["+k+"]", v)
		}
	}
	if len(params) > 0 {
		path += "?" + params.Encode()
	}

	return c.httpClient.Get(ctx, path, nil)
}

// This endpoint returns a list of access token usage on the account.
func (c *Client) ListAccessTokenUsage(ctx context.Context, opts *ListAccessTokenUsageOptions) ([]*schemas.AccessTokenUsage, *client.Response, error) {
	httpResp, err := c.ListAccessTokenUsageRaw(ctx, opts)
	if err != nil {
		return nil, nil, err
	}
	defer httpResp.Body.Close()

	resp := &client.Response{Response: httpResp}

	var result struct {
		Data []schemas.AccessTokenUsage `json:"data"`
		Meta struct {
			Pagination *client.Pagination `json:"pagination"`
		} `json:"meta"`
		Included []map[string]interface{} `json:"included"`
	}
	if err := json.NewDecoder(httpResp.Body).Decode(&result); err != nil {
		return nil, resp, fmt.Errorf("failed to decode response: %w", err)
	}

	resources := make([]*schemas.AccessTokenUsage, len(result.Data))
	for i := range result.Data {
		resources[i] = &result.Data[i]
	}
	resp.Pagination = result.Meta.Pagination
	return resources, resp, nil
}

// ListAccessTokenUsageIter returns an iterator for paginated results using Go 1.23+ range over iter.Seq2 feature.
// This is the recommended and simple way to iterate through all the results.
//
// The iterator automatically fetches pages as needed and handles errors inline.
//
// Example:
//
//	for item, err := range client.AccessTokenUsage.ListAccessTokenUsageIter(ctx, opts) {
//	    if err != nil {
//	        return err
//	    }
//	    // Process item
//	}
func (c *Client) ListAccessTokenUsageIter(ctx context.Context, opts *ListAccessTokenUsageOptions) iter.Seq2[schemas.AccessTokenUsage, error] {
	return func(yield func(schemas.AccessTokenUsage, error) bool) {
		// Determine page size from opts or use default
		pageSize := 20
		if opts != nil && opts.PageSize > 0 {
			pageSize = opts.PageSize
		}

		pageNum := 1

		for {
			// Check context
			if err := ctx.Err(); err != nil {
				yield(schemas.AccessTokenUsage{}, err)
				return
			}

			// Copy options and set page number
			pageOpts := &ListAccessTokenUsageOptions{}
			if opts != nil {
				*pageOpts = *opts
			}
			pageOpts.PageNumber = pageNum
			pageOpts.PageSize = pageSize

			// Fetch page
			items, resp, err := c.ListAccessTokenUsage(ctx, pageOpts)
			if err != nil {
				yield(schemas.AccessTokenUsage{}, err)
				return
			}

			// Yield each item
			for _, item := range items {
				if !yield(*item, nil) {
					return // Consumer requested early exit
				}
			}

			// Check if there are more pages
			if resp.Pagination == nil || resp.Pagination.NextPage == nil {
				break
			}

			pageNum++
		}
	}
}

// ListAccessTokenUsagePaged returns a stateful iterator with access to pagination metadata.
// Use this when you need PageInfo(), Remaining(), or Collect() methods.
//
// Example:
//
//	iter := client.AccessTokenUsage.ListAccessTokenUsagePaged(ctx, opts)
//	for iter.Next() {
//	    item := iter.Value()
//	    fmt.Print(iter.PageInfo()) // -> "page 2/5"
//	    fmt.Printf("%d items remaining", iter.Remaining())
//	}
//	if err := iter.Err(); err != nil {
//	    // Handle error
//	}
func (c *Client) ListAccessTokenUsagePaged(ctx context.Context, opts *ListAccessTokenUsageOptions) *client.Iterator[schemas.AccessTokenUsage] {
	// Determine page size from opts or use default
	pageSize := 20
	if opts != nil && opts.PageSize > 0 {
		pageSize = opts.PageSize
	}

	// Fetch function that will be called for each page
	fetchPage := func(ctx context.Context, pageNum int) ([]*schemas.AccessTokenUsage, *client.Pagination, error) {
		// Copy options and set page number
		pageOpts := &ListAccessTokenUsageOptions{}
		if opts != nil {
			*pageOpts = *opts
		}
		pageOpts.PageNumber = pageNum
		pageOpts.PageSize = pageSize

		// Call the actual list method
		items, resp, err := c.ListAccessTokenUsage(ctx, pageOpts)
		if err != nil {
			return nil, nil, err
		}

		return items, resp.Pagination, nil
	}

	return client.NewIterator[schemas.AccessTokenUsage](ctx, pageSize, fetchPage)
}

// ListAccessTokenUsageOptions holds optional parameters for ListAccessTokenUsage
type ListAccessTokenUsageOptions struct {
	// Format of the response. It can be 'json' or 'csv'.
	Format string
	// Page number.
	PageNumber int
	// Page size.
	PageSize int
	// Query by token and user email
	Query string
	// The comma-separated list of attributes.
	Sort   []string
	Filter map[string]string
}
