// Code generated by scalr-gen. DO NOT EDIT.

package module_test_provider_configuration_link

import (
	"context"
	"encoding/json"
	"fmt"
	"iter"
	"net/url"
	"strings"

	"github.com/scalr/go-scalr/v2/scalr/client"
	"github.com/scalr/go-scalr/v2/scalr/schemas"
)

// Client provides access to ModuleTestProviderConfigurationLink operations
type Client struct {
	httpClient *client.HTTPClient
}

// New creates a new ModuleTestProviderConfigurationLink client
func New(httpClient *client.HTTPClient) *Client {
	return &Client{httpClient: httpClient}
}

// Attach a Provider Configuration to the Module Test Configuration.
func (c *Client) CreateModuleTestProviderConfigurationLinkRaw(ctx context.Context, testConfiguration string, req *schemas.ModuleTestProviderConfigurationLinkRequest) (*client.Response, error) {
	path := "/test-configurations/{test_configuration}/provider-configuration-links"
	path = strings.ReplaceAll(path, "{test_configuration}", url.PathEscape(testConfiguration))

	// Wrap request in JSON:API envelope
	body := map[string]interface{}{"data": req}
	httpResp, err := c.httpClient.Post(ctx, path, body, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// Attach a Provider Configuration to the Module Test Configuration.
func (c *Client) CreateModuleTestProviderConfigurationLink(ctx context.Context, testConfiguration string, req *schemas.ModuleTestProviderConfigurationLinkRequest) (*schemas.ModuleTestProviderConfigurationLink, error) {
	resp, err := c.CreateModuleTestProviderConfigurationLinkRaw(ctx, testConfiguration, req)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()

	var result struct {
		Data     schemas.ModuleTestProviderConfigurationLink `json:"data"`
		Included []map[string]interface{}                    `json:"included"`
	}
	if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
		return nil, fmt.Errorf("failed to decode response: %w", err)
	}

	// Populate included resources into relationships
	if len(result.Included) > 0 {
		result.Data.Relationships.PopulateIncludes(result.Included)
	}
	return &result.Data, nil
}

// The endpoint deletes a Module Test Provider Configuration Link by ID.
func (c *Client) DeleteModuleTestProviderConfigurationLinkRaw(ctx context.Context, moduleTestProviderConfigurationLink string) (*client.Response, error) {
	path := "/module-test-provider-configuration-links/{module_test_provider_configuration_link}"
	path = strings.ReplaceAll(path, "{module_test_provider_configuration_link}", url.PathEscape(moduleTestProviderConfigurationLink))

	httpResp, err := c.httpClient.Delete(ctx, path, nil, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// The endpoint deletes a Module Test Provider Configuration Link by ID.
func (c *Client) DeleteModuleTestProviderConfigurationLink(ctx context.Context, moduleTestProviderConfigurationLink string) error {
	resp, err := c.DeleteModuleTestProviderConfigurationLinkRaw(ctx, moduleTestProviderConfigurationLink)
	if err != nil {
		return err
	}
	defer resp.Body.Close()

	return nil
}

// Show details of a specific Module Test Provider Configuration Link.
func (c *Client) GetModuleTestProviderConfigurationLinkRaw(ctx context.Context, moduleTestProviderConfigurationLink string, opts *GetModuleTestProviderConfigurationLinkOptions) (*client.Response, error) {
	path := "/module-test-provider-configuration-links/{module_test_provider_configuration_link}"
	path = strings.ReplaceAll(path, "{module_test_provider_configuration_link}", url.PathEscape(moduleTestProviderConfigurationLink))

	params := url.Values{}
	if opts != nil {
		if len(opts.Include) > 0 {
			params.Set("include", strings.Join(opts.Include, ","))
		}
		// Sparse fieldsets
		for resourceType, fields := range opts.Fields {
			params.Set("fields["+resourceType+"]", fields)
		}
		// Add filters (keys should be full parameter names like "filter[account]")
		for k, v := range opts.Filters {
			params.Set(k, v)
		}
	}
	if len(params) > 0 {
		path += "?" + params.Encode()
	}

	httpResp, err := c.httpClient.Get(ctx, path, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// Show details of a specific Module Test Provider Configuration Link.
func (c *Client) GetModuleTestProviderConfigurationLink(ctx context.Context, moduleTestProviderConfigurationLink string, opts *GetModuleTestProviderConfigurationLinkOptions) (*schemas.ModuleTestProviderConfigurationLink, error) {
	resp, err := c.GetModuleTestProviderConfigurationLinkRaw(ctx, moduleTestProviderConfigurationLink, opts)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()

	var result struct {
		Data     schemas.ModuleTestProviderConfigurationLink `json:"data"`
		Included []map[string]interface{}                    `json:"included"`
	}
	if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
		return nil, fmt.Errorf("failed to decode response: %w", err)
	}

	// Populate included resources into relationships
	if len(result.Included) > 0 {
		result.Data.Relationships.PopulateIncludes(result.Included)
	}
	return &result.Data, nil
}

// GetModuleTestProviderConfigurationLinkOptions holds optional parameters for GetModuleTestProviderConfigurationLink
type GetModuleTestProviderConfigurationLinkOptions struct {
	// The comma-separated list of relationship paths.
	Include []string
	// Fields specifies which attributes to return for each resource type.
	Fields map[string]string
	// Filters maps filter keys to their values.
	// Use the Filter* constants defined in this package.
	Filters map[string]string
}

// This endpoint returns a list of Provider Configuration links to Module Test Configurations.
func (c *Client) ListModuleTestProviderConfigurationLinksRaw(ctx context.Context, testConfiguration string, opts *ListModuleTestProviderConfigurationLinksOptions) (*client.Response, error) {
	path := "/test-configurations/{test_configuration}/provider-configuration-links"
	path = strings.ReplaceAll(path, "{test_configuration}", url.PathEscape(testConfiguration))

	params := url.Values{}
	if opts != nil {
		if opts.PageNumber > 0 {
			params.Set("page[number]", fmt.Sprintf("%d", opts.PageNumber))
		}
		if opts.PageSize > 0 {
			params.Set("page[size]", fmt.Sprintf("%d", opts.PageSize))
		}
		if len(opts.Include) > 0 {
			params.Set("include", strings.Join(opts.Include, ","))
		}
		// Sparse fieldsets
		for resourceType, fields := range opts.Fields {
			params.Set("fields["+resourceType+"]", fields)
		}
		// Add filters (keys should be full parameter names like "filter[account]")
		for k, v := range opts.Filters {
			params.Set(k, v)
		}
	}
	if len(params) > 0 {
		path += "?" + params.Encode()
	}

	httpResp, err := c.httpClient.Get(ctx, path, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// This endpoint returns a list of Provider Configuration links to Module Test Configurations.
func (c *Client) ListModuleTestProviderConfigurationLinks(ctx context.Context, testConfiguration string, opts *ListModuleTestProviderConfigurationLinksOptions) ([]*schemas.ModuleTestProviderConfigurationLink, error) {
	resp, err := c.ListModuleTestProviderConfigurationLinksRaw(ctx, testConfiguration, opts)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()

	var result struct {
		Data []schemas.ModuleTestProviderConfigurationLink `json:"data"`
		Meta struct {
			Pagination *client.Pagination `json:"pagination"`
		} `json:"meta"`
		Included []map[string]interface{} `json:"included"`
	}
	if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
		return nil, fmt.Errorf("failed to decode response: %w", err)
	}

	resources := make([]*schemas.ModuleTestProviderConfigurationLink, len(result.Data))
	for i := range result.Data {
		resources[i] = &result.Data[i]
		// Populate included resources into relationships
		if len(result.Included) > 0 {
			resources[i].Relationships.PopulateIncludes(result.Included)
		}
	}
	return resources, nil
}

// ListModuleTestProviderConfigurationLinksIter returns an iterator for paginated results using Go 1.23+ range over iter.Seq2 feature.
// This is the recommended and simple way to iterate through all the results.
//
// The iterator automatically fetches pages as needed and handles errors inline.
//
// Example:
//
//	for item, err := range client.ModuleTestProviderConfigurationLink.ListModuleTestProviderConfigurationLinksIter(ctx, testConfiguration, opts) {
//	    if err != nil {
//	        return err
//	    }
//	    // Process item
//	}
func (c *Client) ListModuleTestProviderConfigurationLinksIter(ctx context.Context, testConfiguration string, opts *ListModuleTestProviderConfigurationLinksOptions) iter.Seq2[schemas.ModuleTestProviderConfigurationLink, error] {
	return func(yield func(schemas.ModuleTestProviderConfigurationLink, error) bool) {
		// Determine page size from opts or use default
		pageSize := 20
		if opts != nil && opts.PageSize > 0 {
			pageSize = opts.PageSize
		}

		pageNum := 1

		for {
			// Check context
			if err := ctx.Err(); err != nil {
				yield(schemas.ModuleTestProviderConfigurationLink{}, err)
				return
			}

			// Copy options and set page number
			pageOpts := &ListModuleTestProviderConfigurationLinksOptions{}
			if opts != nil {
				*pageOpts = *opts
			}
			pageOpts.PageNumber = pageNum
			pageOpts.PageSize = pageSize

			// Fetch page using Raw method to get pagination metadata
			resp, err := c.ListModuleTestProviderConfigurationLinksRaw(ctx, testConfiguration, pageOpts)
			if err != nil {
				yield(schemas.ModuleTestProviderConfigurationLink{}, err)
				return
			}

			// Decode response
			var result struct {
				Data []schemas.ModuleTestProviderConfigurationLink `json:"data"`
				Meta struct {
					Pagination *client.Pagination `json:"pagination"`
				} `json:"meta"`
				Included []map[string]interface{} `json:"included"`
			}
			decodeErr := json.NewDecoder(resp.Body).Decode(&result)
			resp.Body.Close()
			if decodeErr != nil {
				yield(schemas.ModuleTestProviderConfigurationLink{}, fmt.Errorf("failed to decode response: %w", decodeErr))
				return
			}

			// Yield each item
			for i := range result.Data {
				// Populate included resources into relationships
				if len(result.Included) > 0 {
					result.Data[i].Relationships.PopulateIncludes(result.Included)
				}
				if !yield(result.Data[i], nil) {
					return // Consumer requested early exit
				}
			}

			// Check if there are more pages
			if result.Meta.Pagination == nil || result.Meta.Pagination.NextPage == nil {
				break
			}

			pageNum++
		}
	}
}

// ListModuleTestProviderConfigurationLinksPaged returns a stateful iterator with access to pagination metadata.
// Use this when you need PageInfo(), Remaining(), or Collect() methods.
//
// Example:
//
//	iter := client.ModuleTestProviderConfigurationLink.ListModuleTestProviderConfigurationLinksPaged(ctx, testConfiguration, opts)
//	for iter.Next() {
//	    item := iter.Value()
//	    fmt.Print(iter.PageInfo()) // -> "page 2/5"
//	    fmt.Printf("%d items remaining", iter.Remaining())
//	}
//	if err := iter.Err(); err != nil {
//	    // Handle error
//	}
func (c *Client) ListModuleTestProviderConfigurationLinksPaged(ctx context.Context, testConfiguration string, opts *ListModuleTestProviderConfigurationLinksOptions) *client.Iterator[schemas.ModuleTestProviderConfigurationLink] {
	// Determine page size from opts or use default
	pageSize := 20
	if opts != nil && opts.PageSize > 0 {
		pageSize = opts.PageSize
	}

	// Fetch function that will be called for each page
	fetchPage := func(ctx context.Context, pageNum int) ([]*schemas.ModuleTestProviderConfigurationLink, *client.Pagination, error) {
		// Copy options and set page number
		pageOpts := &ListModuleTestProviderConfigurationLinksOptions{}
		if opts != nil {
			*pageOpts = *opts
		}
		pageOpts.PageNumber = pageNum
		pageOpts.PageSize = pageSize

		// Call the Raw method to get pagination metadata
		resp, err := c.ListModuleTestProviderConfigurationLinksRaw(ctx, testConfiguration, pageOpts)
		if err != nil {
			return nil, nil, err
		}
		defer resp.Body.Close()

		// Decode response
		var result struct {
			Data []schemas.ModuleTestProviderConfigurationLink `json:"data"`
			Meta struct {
				Pagination *client.Pagination `json:"pagination"`
			} `json:"meta"`
			Included []map[string]interface{} `json:"included"`
		}
		if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
			return nil, nil, fmt.Errorf("failed to decode response: %w", err)
		}

		// Convert to slice of pointers and populate includes
		items := make([]*schemas.ModuleTestProviderConfigurationLink, len(result.Data))
		for i := range result.Data {
			items[i] = &result.Data[i]
			// Populate included resources into relationships
			if len(result.Included) > 0 {
				items[i].Relationships.PopulateIncludes(result.Included)
			}
		}

		return items, result.Meta.Pagination, nil
	}

	return client.NewIterator[schemas.ModuleTestProviderConfigurationLink](ctx, pageSize, fetchPage)
}

// ListModuleTestProviderConfigurationLinksOptions holds optional parameters for ListModuleTestProviderConfigurationLinks
type ListModuleTestProviderConfigurationLinksOptions struct {
	// Page number
	PageNumber int
	// Page size
	PageSize int
	// The comma-separated list of relationship paths.
	Include []string
	// Fields specifies which attributes to return for each resource type.
	Fields map[string]string
	// Filters maps filter keys to their values.
	// Use the Filter* constants defined in this package.
	Filters map[string]string
}

// This endpoint allows updates to attributes of an existing Module Test Provider Configuration Link.
func (c *Client) UpdateModuleTestProviderConfigurationLinkRaw(ctx context.Context, moduleTestProviderConfigurationLink string, req *schemas.ModuleTestProviderConfigurationLinkRequest) (*client.Response, error) {
	path := "/module-test-provider-configuration-links/{module_test_provider_configuration_link}"
	path = strings.ReplaceAll(path, "{module_test_provider_configuration_link}", url.PathEscape(moduleTestProviderConfigurationLink))

	// Wrap request in JSON:API envelope
	body := map[string]interface{}{"data": req}
	httpResp, err := c.httpClient.Patch(ctx, path, body, nil)
	if err != nil {
		return nil, err
	}
	return &client.Response{Response: httpResp}, nil
}

// This endpoint allows updates to attributes of an existing Module Test Provider Configuration Link.
func (c *Client) UpdateModuleTestProviderConfigurationLink(ctx context.Context, moduleTestProviderConfigurationLink string, req *schemas.ModuleTestProviderConfigurationLinkRequest) (*schemas.ModuleTestProviderConfigurationLink, error) {
	resp, err := c.UpdateModuleTestProviderConfigurationLinkRaw(ctx, moduleTestProviderConfigurationLink, req)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()

	var result struct {
		Data     schemas.ModuleTestProviderConfigurationLink `json:"data"`
		Included []map[string]interface{}                    `json:"included"`
	}
	if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
		return nil, fmt.Errorf("failed to decode response: %w", err)
	}

	// Populate included resources into relationships
	if len(result.Included) > 0 {
		result.Data.Relationships.PopulateIncludes(result.Included)
	}
	return &result.Data, nil
}
