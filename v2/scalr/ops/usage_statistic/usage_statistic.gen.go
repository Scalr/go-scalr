// Code generated by scalr-gen. DO NOT EDIT.

package usage_statistic

import (
	"context"
	"encoding/json"
	"fmt"
	"net/http"
	"net/url"

	"github.com/scalr/go-scalr/v2/scalr/client"
	"github.com/scalr/go-scalr/v2/scalr/schemas"
)

// Client provides access to UsageStatistic operations
type Client struct {
	httpClient *client.HTTPClient
}

// New creates a new UsageStatistic client
func New(httpClient *client.HTTPClient) *Client {
	return &Client{httpClient: httpClient}
}

// This endpoint returns billing usage statistics for an account within a period of time. Required permission: accounts:billing
func (c *Client) ListUsageStatisticsRaw(ctx context.Context, opts *ListUsageStatisticsOptions) (*http.Response, error) {
	path := "/usage-statistics"

	params := url.Values{}
	if opts != nil {
		// Handle parameter: BreakdownBy (string)
		if opts.BreakdownBy != "" {
			params.Set("breakdown_by", opts.BreakdownBy)
		}
		// Handle parameter: Total (string)
		if opts.Total != "" {
			params.Set("total", opts.Total)
		}
		// Add filters
		for k, v := range opts.Filter {
			params.Set("filter["+k+"]", v)
		}
	}
	if len(params) > 0 {
		path += "?" + params.Encode()
	}

	return c.httpClient.Get(ctx, path, nil)
}

// This endpoint returns billing usage statistics for an account within a period of time. Required permission: accounts:billing
func (c *Client) ListUsageStatistics(ctx context.Context, opts *ListUsageStatisticsOptions) ([]*schemas.UsageStatistic, *client.Response, error) {
	httpResp, err := c.ListUsageStatisticsRaw(ctx, opts)
	if err != nil {
		return nil, nil, err
	}
	defer httpResp.Body.Close()

	resp := &client.Response{Response: httpResp}

	var result struct {
		Data []schemas.UsageStatistic `json:"data"`
		Meta struct {
			Pagination *client.Pagination `json:"pagination"`
		} `json:"meta"`
		Included []map[string]interface{} `json:"included"`
	}
	if err := json.NewDecoder(httpResp.Body).Decode(&result); err != nil {
		return nil, resp, fmt.Errorf("failed to decode response: %w", err)
	}

	resources := make([]*schemas.UsageStatistic, len(result.Data))
	for i := range result.Data {
		resources[i] = &result.Data[i]
		// Populate included resources into relationships
		if len(result.Included) > 0 {
			resources[i].Relationships.PopulateIncludes(result.Included)
		}
	}
	resp.Pagination = result.Meta.Pagination
	return resources, resp, nil
}

// ListUsageStatisticsOptions holds optional parameters for ListUsageStatistics
type ListUsageStatisticsOptions struct {
	// Breakdown by environment or workspace.
	BreakdownBy string
	// Summarize usage statistics by the whole period.
	Total  string
	Filter map[string]string
}
