// Code generated by scalr-gen. DO NOT EDIT.

package schemas

import (
	"encoding/json"
	"time"

	"github.com/scalr/go-scalr/v2/scalr/value"
)

// Response version - used when unmarshalling from API responses
// A Configuration Version describes the version of a Terraform configuration files. Each run is associated with a configuration version. It provides details of the source of the configuration files, the upload status, and the relationships to VCS and the workspace.
type ConfigurationVersion struct {
	ID            string                            `json:"id"`
	Type          string                            `json:"type"`
	Attributes    ConfigurationVersionAttributes    `json:"attributes"`
	Relationships ConfigurationVersionRelationships `json:"relationships"`
}

// GetID returns the resource ID (implements client.ResourceLike)
func (r ConfigurationVersion) GetID() string {
	return r.ID
}

// GetResourceType returns the JSON:API resource type (implements client.ResourceLike)
func (r ConfigurationVersion) GetResourceType() string {
	if r.Type != "" {
		return r.Type
	}
	return "configuration-versions"
}

// ConfigurationVersionAttributes holds the attributes for ConfigurationVersion (response)
type ConfigurationVersionAttributes struct {
	// Indicates if a run should automatically be queued when the configuration has been uploaded.
	AutoQueueRuns bool `json:"auto-queue-runs"`
	// Date/Time the configuration version was created.
	CreatedAt time.Time `json:"created-at"`
	// This field contains the error description, when this configuration version's status is `errored`.
	ErrorMessage *string `json:"error-message"`
	// The input variables definitions. Example: ```js [ { "description": "The name of the CloudSQL database.", "name": "cloudsql_database", "type": "string" }, {"...": ""..."} ] ```
	Inputs []interface{} `json:"inputs"`
	// Indicates the configuration version can only be used to perform dry runs that comprise the plan, cost estimation (if enabled) and policy checks. With this set to `true` the configuration cannot be used for an apply and never requires confirmation.
	IsDry bool `json:"is-dry"`
	// A blob size in bytes.
	Size *int `json:"size"`
	// The origin for the configuration version.
	Source string `json:"source"`
	// The Configuration version's current status. * `pending` - waiting for the configuration files to upload. * `uploaded` - upload successful. At this point if the `auto-queue-runs: true` the new run should be `queued`. * `errored` - uploaded files post processing failed. Attribute `error-message` contains the details.
	Status string `json:"status"`
	// Date/Time of transition to each status that has occurred.
	StatusTimestamps map[string]interface{} `json:"status-timestamps"`
}

// ConfigurationVersionRelationships holds the relationships for ConfigurationVersion (response)
type ConfigurationVersionRelationships struct {
	// When configuration version was uploaded from a VCS, this relation points to the Git commit information
	VcsRevision *VcsRevision `json:"vcs-revision"`
	// The workspace the configuration version belongs to.
	Workspace *Workspace `json:"workspace"`
}

// UnmarshalJSON implements custom unmarshalling for relationships
func (r *ConfigurationVersionRelationships) UnmarshalJSON(data []byte) error {
	// Parse into temporary structure that matches JSON:API format
	var temp map[string]json.RawMessage
	if err := json.Unmarshal(data, &temp); err != nil {
		return err
	}

	if raw, ok := temp["vcs-revision"]; ok {
		// To-one relationship
		var rel struct {
			Data *struct {
				ID   string `json:"id"`
				Type string `json:"type"`
			} `json:"data"`
		}
		if err := json.Unmarshal(raw, &rel); err != nil {
			return err
		}
		if rel.Data != nil {
			r.VcsRevision = &VcsRevision{
				ID:   rel.Data.ID,
				Type: rel.Data.Type,
			}
		}
	}
	if raw, ok := temp["workspace"]; ok {
		// To-one relationship
		var rel struct {
			Data *struct {
				ID   string `json:"id"`
				Type string `json:"type"`
			} `json:"data"`
		}
		if err := json.Unmarshal(raw, &rel); err != nil {
			return err
		}
		if rel.Data != nil {
			r.Workspace = &Workspace{
				ID:   rel.Data.ID,
				Type: rel.Data.Type,
			}
		}
	}
	return nil
}

// PopulateIncludes merges included resources into the relationships
func (r *ConfigurationVersionRelationships) PopulateIncludes(included []map[string]interface{}) {
	if r == nil || len(included) == 0 {
		return
	}

	// Build a map of included resources by type+id for fast lookup
	includedMap := make(map[string]map[string]interface{})
	for _, inc := range included {
		if id, ok := inc["id"].(string); ok {
			if typ, ok := inc["type"].(string); ok {
				key := typ + ":" + id
				includedMap[key] = inc
			}
		}
	}

	// Populate to-one relationship: VcsRevision
	if r.VcsRevision != nil && r.VcsRevision.ID != "" {
		key := r.VcsRevision.Type + ":" + r.VcsRevision.ID
		if fullResource, ok := includedMap[key]; ok {
			// Unmarshal the full resource
			data, _ := json.Marshal(fullResource)
			var full VcsRevision
			if err := json.Unmarshal(data, &full); err == nil {
				r.VcsRevision = &full
			}
		}
	}
	// Populate to-one relationship: Workspace
	if r.Workspace != nil && r.Workspace.ID != "" {
		key := r.Workspace.Type + ":" + r.Workspace.ID
		if fullResource, ok := includedMap[key]; ok {
			// Unmarshal the full resource
			data, _ := json.Marshal(fullResource)
			var full Workspace
			if err := json.Unmarshal(data, &full); err == nil {
				r.Workspace = &full
			}
		}
	}
}

// Request version - used when marshalling for API requests
// A Configuration Version describes the version of a Terraform configuration files. Each run is associated with a configuration version. It provides details of the source of the configuration files, the upload status, and the relationships to VCS and the workspace. (for requests)
type ConfigurationVersionRequest struct {
	ID            string                                   `json:"id,omitempty"`
	Type          string                                   `json:"type,omitempty"`
	Attributes    ConfigurationVersionAttributesRequest    `json:"attributes,omitempty"`
	Relationships ConfigurationVersionRelationshipsRequest `json:"relationships,omitempty"`
}

// MarshalJSON automatically sets the Type field during marshalling
func (r ConfigurationVersionRequest) MarshalJSON() ([]byte, error) {
	type Alias ConfigurationVersionRequest
	if r.Type == "" {
		r.Type = "configuration-versions"
	}
	return json.Marshal((Alias)(r))
}

// GetID returns the resource ID (implements client.ResourceLike)
func (r ConfigurationVersionRequest) GetID() string {
	return r.ID
}

// GetResourceType returns the JSON:API resource type (implements client.ResourceLike)
func (r ConfigurationVersionRequest) GetResourceType() string {
	return "configuration-versions"
}

// ConfigurationVersionAttributesRequest holds the attributes for ConfigurationVersion (request)
type ConfigurationVersionAttributesRequest struct {
	// Indicates if a run should automatically be queued when the configuration has been uploaded.
	AutoQueueRuns *value.Value[bool] `json:"auto-queue-runs,omitempty"`
	// Indicates the configuration version can only be used to perform dry runs that comprise the plan, cost estimation (if enabled) and policy checks. With this set to `true` the configuration cannot be used for an apply and never requires confirmation.
	IsDry *value.Value[bool] `json:"is-dry,omitempty"`
}

// ConfigurationVersionRelationshipsRequest holds the relationships for ConfigurationVersion (request)
type ConfigurationVersionRelationshipsRequest struct {
	// The workspace the configuration version belongs to.
	Workspace *value.Value[Workspace] `json:"workspace,omitempty"`
}
