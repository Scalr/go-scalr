// Code generated by scalr-gen. DO NOT EDIT.

package schemas

import (
	"encoding/json"

	"github.com/scalr/go-scalr/v2/scalr/value"
)

// Response version - used when unmarshalling from API responses
// Represents an create [IAM](https://docs.scalr.io/docs/identity-and-access-management) account user request.
type UserInvite struct {
	ID            string                  `json:"id"`
	Type          string                  `json:"type"`
	Attributes    UserInviteAttributes    `json:"attributes"`
	Relationships UserInviteRelationships `json:"relationships"`
}

// GetID returns the resource ID (implements client.ResourceLike)
func (r UserInvite) GetID() string {
	return r.ID
}

// GetResourceType returns the JSON:API resource type (implements client.ResourceLike)
func (r UserInvite) GetResourceType() string {
	if r.Type != "" {
		return r.Type
	}
	return "users"
}

// UserInviteAttributes holds the attributes for UserInvite (response)
type UserInviteAttributes struct {
	Email string `json:"email"`
	// If set to `true`, email with invite to the account will be sent to the user email.
	SendInvite bool `json:"send-invite"`
}

// UserInviteRelationships holds the relationships for UserInvite (response)
type UserInviteRelationships struct {
	// Assign roles for this user on the account scope.
	Roles []*Role `json:"roles"`
	// Add user to the teams.
	Teams []*Team `json:"teams"`
}

// UnmarshalJSON implements custom unmarshalling for relationships
func (r *UserInviteRelationships) UnmarshalJSON(data []byte) error {
	// Parse into temporary structure that matches JSON:API format
	var temp map[string]json.RawMessage
	if err := json.Unmarshal(data, &temp); err != nil {
		return err
	}

	if raw, ok := temp["roles"]; ok {
		// To-many relationship
		var rel struct {
			Data []struct {
				ID   string `json:"id"`
				Type string `json:"type"`
			} `json:"data"`
		}
		if err := json.Unmarshal(raw, &rel); err != nil {
			return err
		}
		if rel.Data != nil {
			r.Roles = make([]*Role, len(rel.Data))
			for i, d := range rel.Data {
				r.Roles[i] = &Role{
					ID:   d.ID,
					Type: d.Type,
				}
			}
		}
	}
	if raw, ok := temp["teams"]; ok {
		// To-many relationship
		var rel struct {
			Data []struct {
				ID   string `json:"id"`
				Type string `json:"type"`
			} `json:"data"`
		}
		if err := json.Unmarshal(raw, &rel); err != nil {
			return err
		}
		if rel.Data != nil {
			r.Teams = make([]*Team, len(rel.Data))
			for i, d := range rel.Data {
				r.Teams[i] = &Team{
					ID:   d.ID,
					Type: d.Type,
				}
			}
		}
	}
	return nil
}

// PopulateIncludes merges included resources into the relationships
func (r *UserInviteRelationships) PopulateIncludes(included []map[string]interface{}) {
	if r == nil || len(included) == 0 {
		return
	}

	// Build a map of included resources by type+id for fast lookup
	includedMap := make(map[string]map[string]interface{})
	for _, inc := range included {
		if id, ok := inc["id"].(string); ok {
			if typ, ok := inc["type"].(string); ok {
				key := typ + ":" + id
				includedMap[key] = inc
			}
		}
	}

	// Populate to-many relationship: Roles
	if r.Roles != nil {
		for i, resource := range r.Roles {
			if resource != nil && resource.ID != "" {
				key := resource.Type + ":" + resource.ID
				if fullResource, ok := includedMap[key]; ok {
					// Unmarshal the full resource
					data, _ := json.Marshal(fullResource)
					var full Role
					if err := json.Unmarshal(data, &full); err == nil {
						r.Roles[i] = &full
					}
				}
			}
		}
	}
	// Populate to-many relationship: Teams
	if r.Teams != nil {
		for i, resource := range r.Teams {
			if resource != nil && resource.ID != "" {
				key := resource.Type + ":" + resource.ID
				if fullResource, ok := includedMap[key]; ok {
					// Unmarshal the full resource
					data, _ := json.Marshal(fullResource)
					var full Team
					if err := json.Unmarshal(data, &full); err == nil {
						r.Teams[i] = &full
					}
				}
			}
		}
	}
}

// Request version - used when marshalling for API requests
// Represents an create [IAM](https://docs.scalr.io/docs/identity-and-access-management) account user request. (for requests)
type UserInviteRequest struct {
	ID            string                         `json:"id,omitempty"`
	Type          string                         `json:"type,omitempty"`
	Attributes    UserInviteAttributesRequest    `json:"attributes,omitempty"`
	Relationships UserInviteRelationshipsRequest `json:"relationships,omitempty"`
}

// MarshalJSON automatically sets the Type field during marshalling
func (r UserInviteRequest) MarshalJSON() ([]byte, error) {
	type Alias UserInviteRequest
	if r.Type == "" {
		r.Type = "users"
	}
	return json.Marshal((Alias)(r))
}

// GetID returns the resource ID (implements client.ResourceLike)
func (r UserInviteRequest) GetID() string {
	return r.ID
}

// GetResourceType returns the JSON:API resource type (implements client.ResourceLike)
func (r UserInviteRequest) GetResourceType() string {
	return "users"
}

// UserInviteAttributesRequest holds the attributes for UserInvite (request)
type UserInviteAttributesRequest struct {
	Email *value.Value[string] `json:"email,omitempty"`
	// If set to `true`, email with invite to the account will be sent to the user email.
	SendInvite *value.Value[bool] `json:"send-invite,omitempty"`
}

// UserInviteRelationshipsRequest holds the relationships for UserInvite (request)
type UserInviteRelationshipsRequest struct {
	// Assign roles for this user on the account scope.
	Roles *value.Value[[]Role] `json:"roles,omitempty"`
	// Add user to the teams.
	Teams *value.Value[[]Team] `json:"teams,omitempty"`
}
