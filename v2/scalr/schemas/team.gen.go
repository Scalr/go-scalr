// Code generated by scalr-gen. DO NOT EDIT.

package schemas

import (
	"encoding/json"

	"github.com/scalr/go-scalr/v2/scalr/value"
)

// Response version - used when unmarshalling from API responses
// A [IAM](https://docs.scalr.io/docs/identity-and-access-management) team is used to organize users into logical units. A team can not be used to group service accounts, only standard users.
type Team struct {
	ID            string            `json:"id"`
	Type          string            `json:"type"`
	Attributes    TeamAttributes    `json:"attributes"`
	Relationships TeamRelationships `json:"relationships"`
}

// GetID returns the resource ID (implements client.ResourceLike)
func (r Team) GetID() string {
	return r.ID
}

// GetResourceType returns the JSON:API resource type (implements client.ResourceLike)
func (r Team) GetResourceType() string {
	if r.Type != "" {
		return r.Type
	}
	return "teams"
}

// TeamAttributes holds the attributes for Team (response)
type TeamAttributes struct {
	// The verbose description of the team.
	Description string `json:"description"`
	// The name of the team.
	Name string `json:"name"`
}

// TeamRelationships holds the relationships for Team (response)
type TeamRelationships struct {
	// The account this team belongs to.
	Account *Account `json:"account"`
	// Identity provider of the team. Can be internal (Scalr) or external (LDAP/SAML)
	IdentityProvider *IdentityProvider `json:"identity-provider"`
	// The list of users in the team.
	Users []*User `json:"users"`
}

// UnmarshalJSON implements custom unmarshalling for relationships
func (r *TeamRelationships) UnmarshalJSON(data []byte) error {
	// Parse into temporary structure that matches JSON:API format
	var temp map[string]json.RawMessage
	if err := json.Unmarshal(data, &temp); err != nil {
		return err
	}

	if raw, ok := temp["account"]; ok {
		// To-one relationship
		var rel struct {
			Data *struct {
				ID   string `json:"id"`
				Type string `json:"type"`
			} `json:"data"`
		}
		if err := json.Unmarshal(raw, &rel); err != nil {
			return err
		}
		if rel.Data != nil {
			r.Account = &Account{
				ID:   rel.Data.ID,
				Type: rel.Data.Type,
			}
		}
	}
	if raw, ok := temp["identity-provider"]; ok {
		// To-one relationship
		var rel struct {
			Data *struct {
				ID   string `json:"id"`
				Type string `json:"type"`
			} `json:"data"`
		}
		if err := json.Unmarshal(raw, &rel); err != nil {
			return err
		}
		if rel.Data != nil {
			r.IdentityProvider = &IdentityProvider{
				ID:   rel.Data.ID,
				Type: rel.Data.Type,
			}
		}
	}
	if raw, ok := temp["users"]; ok {
		// To-many relationship
		var rel struct {
			Data []struct {
				ID   string `json:"id"`
				Type string `json:"type"`
			} `json:"data"`
		}
		if err := json.Unmarshal(raw, &rel); err != nil {
			return err
		}
		if rel.Data != nil {
			r.Users = make([]*User, len(rel.Data))
			for i, d := range rel.Data {
				r.Users[i] = &User{
					ID:   d.ID,
					Type: d.Type,
				}
			}
		}
	}
	return nil
}

// PopulateIncludes merges included resources into the relationships
func (r *TeamRelationships) PopulateIncludes(included []map[string]interface{}) {
	if r == nil || len(included) == 0 {
		return
	}

	// Build a map of included resources by type+id for fast lookup
	includedMap := make(map[string]map[string]interface{})
	for _, inc := range included {
		if id, ok := inc["id"].(string); ok {
			if typ, ok := inc["type"].(string); ok {
				key := typ + ":" + id
				includedMap[key] = inc
			}
		}
	}

	// Populate to-one relationship: Account
	if r.Account != nil && r.Account.ID != "" {
		key := r.Account.Type + ":" + r.Account.ID
		if fullResource, ok := includedMap[key]; ok {
			// Unmarshal the full resource
			data, _ := json.Marshal(fullResource)
			var full Account
			if err := json.Unmarshal(data, &full); err == nil {
				r.Account = &full
			}
		}
	}
	// Populate to-one relationship: IdentityProvider
	if r.IdentityProvider != nil && r.IdentityProvider.ID != "" {
		key := r.IdentityProvider.Type + ":" + r.IdentityProvider.ID
		if fullResource, ok := includedMap[key]; ok {
			// Unmarshal the full resource
			data, _ := json.Marshal(fullResource)
			var full IdentityProvider
			if err := json.Unmarshal(data, &full); err == nil {
				r.IdentityProvider = &full
			}
		}
	}
	// Populate to-many relationship: Users
	if r.Users != nil {
		for i, resource := range r.Users {
			if resource != nil && resource.ID != "" {
				key := resource.Type + ":" + resource.ID
				if fullResource, ok := includedMap[key]; ok {
					// Unmarshal the full resource
					data, _ := json.Marshal(fullResource)
					var full User
					if err := json.Unmarshal(data, &full); err == nil {
						r.Users[i] = &full
					}
				}
			}
		}
	}
}

// Request version - used when marshalling for API requests
// A [IAM](https://docs.scalr.io/docs/identity-and-access-management) team is used to organize users into logical units. A team can not be used to group service accounts, only standard users. (for requests)
type TeamRequest struct {
	ID            string                   `json:"id,omitempty"`
	Type          string                   `json:"type,omitempty"`
	Attributes    TeamAttributesRequest    `json:"attributes,omitempty"`
	Relationships TeamRelationshipsRequest `json:"relationships,omitempty"`
}

// MarshalJSON automatically sets the Type field during marshalling
func (r TeamRequest) MarshalJSON() ([]byte, error) {
	type Alias TeamRequest
	if r.Type == "" {
		r.Type = "teams"
	}
	return json.Marshal((Alias)(r))
}

// GetID returns the resource ID (implements client.ResourceLike)
func (r TeamRequest) GetID() string {
	return r.ID
}

// GetResourceType returns the JSON:API resource type (implements client.ResourceLike)
func (r TeamRequest) GetResourceType() string {
	return "teams"
}

// TeamAttributesRequest holds the attributes for Team (request)
type TeamAttributesRequest struct {
	// The verbose description of the team.
	Description *value.Value[string] `json:"description,omitempty"`
	// The name of the team.
	Name *value.Value[string] `json:"name,omitempty"`
}

// TeamRelationshipsRequest holds the relationships for Team (request)
type TeamRelationshipsRequest struct {
	// The list of users in the team.
	Users *value.Value[[]User] `json:"users,omitempty"`
}
