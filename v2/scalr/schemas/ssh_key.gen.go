// Code generated by scalr-gen. DO NOT EDIT.

package schemas

import (
	"encoding/json"
	"time"

	"github.com/scalr/go-scalr/v2/scalr/value"
)

// Response version - used when unmarshalling from API responses
// The SSH key resource. This resource represents an SSH key entity.
type SSHKey struct {
	ID            string              `json:"id"`
	Type          string              `json:"type"`
	Attributes    SSHKeyAttributes    `json:"attributes"`
	Relationships SSHKeyRelationships `json:"relationships"`
}

// GetID returns the resource ID (implements client.ResourceLike)
func (r SSHKey) GetID() string {
	return r.ID
}

// GetResourceType returns the JSON:API resource type (implements client.ResourceLike)
func (r SSHKey) GetResourceType() string {
	if r.Type != "" {
		return r.Type
	}
	return "account-ssh-keys"
}

// SSHKeyAttributes holds the attributes for SSHKey (response)
type SSHKeyAttributes struct {
	// The date and time when the SSH key was created.
	CreatedAt time.Time `json:"created-at"`
	// Indicates whether the SSH key is in use.
	InUse bool `json:"in-use"`
	// Indicates whether the SSH key is available in any environment of the account without directly linking it.
	IsShared bool `json:"is-shared"`
	// The name of the SSH key.
	Name string `json:"name"`
	// The private key of the SSH key.
	PrivateKey string `json:"private-key"`
}

// SSHKeyRelationships holds the relationships for SSHKey (response)
type SSHKeyRelationships struct {
	// The reference to the account that the SSH key belongs to.
	Account *Account `json:"account"`
	// The list of environments where the SSH key can be used.
	Environments []*Environment `json:"environments"`
}

// UnmarshalJSON implements custom unmarshalling for relationships
func (r *SSHKeyRelationships) UnmarshalJSON(data []byte) error {
	// Parse into temporary structure that matches JSON:API format
	var temp map[string]json.RawMessage
	if err := json.Unmarshal(data, &temp); err != nil {
		return err
	}

	if raw, ok := temp["account"]; ok {
		// To-one relationship
		var rel struct {
			Data *struct {
				ID   string `json:"id"`
				Type string `json:"type"`
			} `json:"data"`
		}
		if err := json.Unmarshal(raw, &rel); err != nil {
			return err
		}
		if rel.Data != nil {
			r.Account = &Account{
				ID:   rel.Data.ID,
				Type: rel.Data.Type,
			}
		}
	}
	if raw, ok := temp["environments"]; ok {
		// To-many relationship
		var rel struct {
			Data []struct {
				ID   string `json:"id"`
				Type string `json:"type"`
			} `json:"data"`
		}
		if err := json.Unmarshal(raw, &rel); err != nil {
			return err
		}
		if rel.Data != nil {
			r.Environments = make([]*Environment, len(rel.Data))
			for i, d := range rel.Data {
				r.Environments[i] = &Environment{
					ID:   d.ID,
					Type: d.Type,
				}
			}
		}
	}
	return nil
}

// PopulateIncludes merges included resources into the relationships
func (r *SSHKeyRelationships) PopulateIncludes(included []map[string]interface{}) {
	if r == nil || len(included) == 0 {
		return
	}

	// Build a map of included resources by type+id for fast lookup
	includedMap := make(map[string]map[string]interface{})
	for _, inc := range included {
		if id, ok := inc["id"].(string); ok {
			if typ, ok := inc["type"].(string); ok {
				key := typ + ":" + id
				includedMap[key] = inc
			}
		}
	}

	// Populate to-one relationship: Account
	if r.Account != nil && r.Account.ID != "" {
		key := r.Account.Type + ":" + r.Account.ID
		if fullResource, ok := includedMap[key]; ok {
			// Unmarshal the full resource
			data, _ := json.Marshal(fullResource)
			var full Account
			if err := json.Unmarshal(data, &full); err == nil {
				r.Account = &full
			}
		}
	}
	// Populate to-many relationship: Environments
	if r.Environments != nil {
		for i, resource := range r.Environments {
			if resource != nil && resource.ID != "" {
				key := resource.Type + ":" + resource.ID
				if fullResource, ok := includedMap[key]; ok {
					// Unmarshal the full resource
					data, _ := json.Marshal(fullResource)
					var full Environment
					if err := json.Unmarshal(data, &full); err == nil {
						r.Environments[i] = &full
					}
				}
			}
		}
	}
}

// Request version - used when marshalling for API requests
// The SSH key resource. This resource represents an SSH key entity. (for requests)
type SSHKeyRequest struct {
	ID            string                     `json:"id,omitempty"`
	Type          string                     `json:"type,omitempty"`
	Attributes    SSHKeyAttributesRequest    `json:"attributes,omitempty"`
	Relationships SSHKeyRelationshipsRequest `json:"relationships,omitempty"`
}

// MarshalJSON automatically sets the Type field during marshalling
func (r SSHKeyRequest) MarshalJSON() ([]byte, error) {
	type Alias SSHKeyRequest
	if r.Type == "" {
		r.Type = "account-ssh-keys"
	}
	return json.Marshal((Alias)(r))
}

// GetID returns the resource ID (implements client.ResourceLike)
func (r SSHKeyRequest) GetID() string {
	return r.ID
}

// GetResourceType returns the JSON:API resource type (implements client.ResourceLike)
func (r SSHKeyRequest) GetResourceType() string {
	return "account-ssh-keys"
}

// SSHKeyAttributesRequest holds the attributes for SSHKey (request)
type SSHKeyAttributesRequest struct {
	// Indicates whether the SSH key is available in any environment of the account without directly linking it.
	IsShared *value.Value[bool] `json:"is-shared,omitempty"`
	// The name of the SSH key.
	Name *value.Value[string] `json:"name,omitempty"`
	// The private key of the SSH key.
	PrivateKey *value.Value[string] `json:"private-key,omitempty"`
}

// SSHKeyRelationshipsRequest holds the relationships for SSHKey (request)
type SSHKeyRelationshipsRequest struct {
	// The reference to the account that the SSH key belongs to.
	Account *value.Value[Account] `json:"account,omitempty"`
	// The list of environments where the SSH key can be used.
	Environments *value.Value[[]Environment] `json:"environments,omitempty"`
}
