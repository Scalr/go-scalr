// Code generated by scalr-gen. DO NOT EDIT.

package schemas

import (
	"encoding/json"
	"time"

	"github.com/scalr/go-scalr/v2/scalr/value"
)

// Response version - used when unmarshalling from API responses
// Represents a service account definition. A service account is a special type of account intended to represent a non-human user that needs to authenticate and be authorized to access data in Scalr APIs.
type ServiceAccount struct {
	ID            string                      `json:"id"`
	Type          string                      `json:"type"`
	Attributes    ServiceAccountAttributes    `json:"attributes"`
	Relationships ServiceAccountRelationships `json:"relationships"`
}

// GetID returns the resource ID (implements client.ResourceLike)
func (r ServiceAccount) GetID() string {
	return r.ID
}

// GetResourceType returns the JSON:API resource type (implements client.ResourceLike)
func (r ServiceAccount) GetResourceType() string {
	if r.Type != "" {
		return r.Type
	}
	return "service-accounts"
}

// ServiceAccountAttributes holds the attributes for ServiceAccount (response)
type ServiceAccountAttributes struct {
	CreatedAt time.Time `json:"created-at"`
	// The service account description.
	Description *string `json:"description"`
	// A read-only field which is generated when a service account is created. Consists of `<name>@<account-domain>.scalr.io`
	Email string `json:"email"`
	// The service account name. The service account email will be created using this name.
	Name string `json:"name"`
	// The service account status. Can be: `Active`, `Inactive`.
	Status string `json:"status"`
}

// ServiceAccountRelationships holds the relationships for ServiceAccount (response)
type ServiceAccountRelationships struct {
	// The account ID, the service account belongs to.
	Account *Account `json:"account"`
	// The user who created the service account.
	CreatedBy *User `json:"created-by"`
	// The teams, the service account belongs to.
	Owners []*Team `json:"owners"`
}

// UnmarshalJSON implements custom unmarshalling for relationships
func (r *ServiceAccountRelationships) UnmarshalJSON(data []byte) error {
	// Parse into temporary structure that matches JSON:API format
	var temp map[string]json.RawMessage
	if err := json.Unmarshal(data, &temp); err != nil {
		return err
	}

	if raw, ok := temp["account"]; ok {
		// To-one relationship
		var rel struct {
			Data *struct {
				ID   string `json:"id"`
				Type string `json:"type"`
			} `json:"data"`
		}
		if err := json.Unmarshal(raw, &rel); err != nil {
			return err
		}
		if rel.Data != nil {
			r.Account = &Account{
				ID:   rel.Data.ID,
				Type: rel.Data.Type,
			}
		}
	}
	if raw, ok := temp["created-by"]; ok {
		// To-one relationship
		var rel struct {
			Data *struct {
				ID   string `json:"id"`
				Type string `json:"type"`
			} `json:"data"`
		}
		if err := json.Unmarshal(raw, &rel); err != nil {
			return err
		}
		if rel.Data != nil {
			r.CreatedBy = &User{
				ID:   rel.Data.ID,
				Type: rel.Data.Type,
			}
		}
	}
	if raw, ok := temp["owners"]; ok {
		// To-many relationship
		var rel struct {
			Data []struct {
				ID   string `json:"id"`
				Type string `json:"type"`
			} `json:"data"`
		}
		if err := json.Unmarshal(raw, &rel); err != nil {
			return err
		}
		if rel.Data != nil {
			r.Owners = make([]*Team, len(rel.Data))
			for i, d := range rel.Data {
				r.Owners[i] = &Team{
					ID:   d.ID,
					Type: d.Type,
				}
			}
		}
	}
	return nil
}

// PopulateIncludes merges included resources into the relationships
func (r *ServiceAccountRelationships) PopulateIncludes(included []map[string]interface{}) {
	if r == nil || len(included) == 0 {
		return
	}

	// Build a map of included resources by type+id for fast lookup
	includedMap := make(map[string]map[string]interface{})
	for _, inc := range included {
		if id, ok := inc["id"].(string); ok {
			if typ, ok := inc["type"].(string); ok {
				key := typ + ":" + id
				includedMap[key] = inc
			}
		}
	}

	// Populate to-one relationship: Account
	if r.Account != nil && r.Account.ID != "" {
		key := r.Account.Type + ":" + r.Account.ID
		if fullResource, ok := includedMap[key]; ok {
			// Unmarshal the full resource
			data, _ := json.Marshal(fullResource)
			var full Account
			if err := json.Unmarshal(data, &full); err == nil {
				r.Account = &full
			}
		}
	}
	// Populate to-one relationship: CreatedBy
	if r.CreatedBy != nil && r.CreatedBy.ID != "" {
		key := r.CreatedBy.Type + ":" + r.CreatedBy.ID
		if fullResource, ok := includedMap[key]; ok {
			// Unmarshal the full resource
			data, _ := json.Marshal(fullResource)
			var full User
			if err := json.Unmarshal(data, &full); err == nil {
				r.CreatedBy = &full
			}
		}
	}
	// Populate to-many relationship: Owners
	if r.Owners != nil {
		for i, resource := range r.Owners {
			if resource != nil && resource.ID != "" {
				key := resource.Type + ":" + resource.ID
				if fullResource, ok := includedMap[key]; ok {
					// Unmarshal the full resource
					data, _ := json.Marshal(fullResource)
					var full Team
					if err := json.Unmarshal(data, &full); err == nil {
						r.Owners[i] = &full
					}
				}
			}
		}
	}
}

// Request version - used when marshalling for API requests
// Represents a service account definition. A service account is a special type of account intended to represent a non-human user that needs to authenticate and be authorized to access data in Scalr APIs. (for requests)
type ServiceAccountRequest struct {
	ID            string                             `json:"id,omitempty"`
	Type          string                             `json:"type,omitempty"`
	Attributes    ServiceAccountAttributesRequest    `json:"attributes,omitempty"`
	Relationships ServiceAccountRelationshipsRequest `json:"relationships,omitempty"`
}

// MarshalJSON automatically sets the Type field during marshalling
func (r ServiceAccountRequest) MarshalJSON() ([]byte, error) {
	type Alias ServiceAccountRequest
	if r.Type == "" {
		r.Type = "service-accounts"
	}
	return json.Marshal((Alias)(r))
}

// GetID returns the resource ID (implements client.ResourceLike)
func (r ServiceAccountRequest) GetID() string {
	return r.ID
}

// GetResourceType returns the JSON:API resource type (implements client.ResourceLike)
func (r ServiceAccountRequest) GetResourceType() string {
	return "service-accounts"
}

// ServiceAccountAttributesRequest holds the attributes for ServiceAccount (request)
type ServiceAccountAttributesRequest struct {
	// The service account description.
	Description *value.Value[string] `json:"description,omitempty"`
	// The service account name. The service account email will be created using this name.
	Name *value.Value[string] `json:"name,omitempty"`
	// The service account status. Can be: `Active`, `Inactive`.
	Status *value.Value[string] `json:"status,omitempty"`
}

// ServiceAccountRelationshipsRequest holds the relationships for ServiceAccount (request)
type ServiceAccountRelationshipsRequest struct {
	// The account ID, the service account belongs to.
	Account *value.Value[Account] `json:"account,omitempty"`
	// The teams, the service account belongs to.
	Owners *value.Value[[]Team] `json:"owners,omitempty"`
}
